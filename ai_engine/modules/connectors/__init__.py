"""Module connectors — sources de connaissance unifiées (extension RAG).

Ingère des documents / historiques de chat / e-mails / fichiers / notes dans le RAG (avec
métadonnées de source) et fournit une **recherche unifiée cross-namespace** (RAG). Construit
au-dessus du module `rag` existant (aucune duplication). Offline (repli embeddings hash).
"""
