# Base de connaissance — Écosystème Lunziko (vue Lunziko AI Engine)

> ⚙️ **FICHIER AUTO-GÉNÉRÉ — NE PAS ÉDITER À LA MAIN.**
> Alimenté depuis le registre maître : `C:\Users\Joe\Desktop\Lunziko\REGISTRE_ECOSYSTEME_LUNZIKO.md`
> Version du registre : **1.8 — 23 août 2026 (**Graphics Engine — Amélioration 2 « moteur universel »** : spec `docs/SPECIFICATION_LGE_AMELIORATION_2.md` = 42 capacités manquantes en 14 modules critiques pour servir VidiaPub + Live Stream + Lunziko CAD v2 [temps réel 2D/vidéo/audio, backends GPU DX12/Metal/Vulkan/WebGPU, chroma/segmentation IA, texte pro, vectoriel avancé, CAO NURBS/kernel, BIM clash/rule, simulation FEA/thermique/fluides, path tracing PBR/HDR, IA graphique NL→CAD/BIM, SceneOptimizer + cache GPU partagé, multi-sorties 16:9+9:16] ; sources externes clean-room à intégrer : DiligentEngine [Apache-2.0], NVIDIA NVRHI [MIT], NVIDIA-RTX [RTX Remix/PhysX/Flow/Streamline], openh264 [BSD], anime4k [MIT], 4KAgent, litecode, libde265 [⚠️GPL/HEVC process séparé] ; services requis = AI Engine + SDKs GPU + codecs. NON codé.)** · généré le 2026-08-23.
> Régénérer : `python scripts/build_ecosystem_kb.py --project "Lunziko AI Engine"` (règle §0.4 : à chaque lancement).

## Rôle de Lunziko AI Engine (extrait du registre)

- **Catégorie :** IA autonome (indépendante de Platform ; Platform = client optionnel). Objectif : **accompagner l'utilisateur** sur toutes les applications. **Architecture = LAIA (Lunziko AI Intelligence Architecture, Multi-Brain/Multi-Engine)** : un Orchestrator coordonne des **Brains** (cerveaux spécialisés), des **Engines** (les modules) et des **Tools** — construit AU-DESSUS de l'existant (cf. `LAIA_ARCHITECTURE.md`).
- **Statut :** code `lunziko-ai-engine/` (FastAPI, cœur Python, persistance enfichable locale). Modules **A-0→A-6 + A-8 + A-12 + A-13 + neural + data + assistant + handoff + tools (A-4b) + mcp (A-7) + context (A-14→A-16) + feedback (A-18) + catalog (A-17) + automation (A-10) + actions + **orchestrator (LAIA AI-CORE)** + **codeexec (A-11)** + **graphics (branchement)** + **connectors (RAG unifié)** + **safety (garde-fous)** + **search (web keyless)** + **voix V-1 réelle (TTS Piper + STT Whisper)** livrés** (29 modules ; Couche de Contexte A-14→A-18 COMPLÈTE ; architecture LAIA Multi-Brain/Multi-Engine + App Requirements + Evaluation + Brain-to-Brain + Code Execution sûr + Graphics Engine connecté (REST, validé) + connecteurs RAG + Safety Engine + **orchestrateur→Graphics délégué** (validé LIVE : 3d→delegated, 22 agents) + **Search web DuckDuckGo sans clé** + **Voix V-1 TTS+STT réels** (round-trip validé)). Suite pytest offline (**95 tests**) + CI GitHub Actions. **SDKs A-9 : TS + Swift + Kotlin + Dart** (Dart validé `dart analyze`). **🐳 DÉPLOIEMENT DOCKER (2026-08-16) : image `jaisebukadilu/lunziko-ai-engine:latest`+`:0.1.0` BUILDÉE + smoke-test `/health` OK (28 modules actifs) + PUBLIÉE sur Docker Hub** (Dockerfile+docker-compose+DEPLOY.md ; `docker pull jaisebukadilu/lunziko-ai-engine`). py_compile OK + tests offline (E2E écosystème : sync v1.2, 11 apps ; E2E activité : chiffrement AES-GCM ; E2E neural : routeur d'intention **HYBRIDE 12/12** vs 1/12 mots-clés bruts). Spec **Couche de Contexte Unifié** `CONTEXT_LAYER_ARCHITECTURE.md`. **+ LLM natif from scratch `lunziko-llm/`** (NumPy gradient-checké + **backend PyTorch GPU-ready** — même archi RoPE/RMSNorm/GQA/SwiGLU/poids liés, entraîné offline loss 6.0→0.11) branché comme provider `lunziko` ; dépôt `github.com/jaisebukadilu/Lunziko_LLM`. **SOUS GIT + GITHUB (2026-08-14)** : 2 dépôts poussés — `lunziko-ai-engine/` (83 fichiers) sur **`github.com/jaisebukadilu/Lunziko_Ai_Engine`** (main @ 43e0967, 17 commits, ~136 fichiers py) + `lunziko-llm/` (13 fichiers) sur **`github.com/jaisebukadilu/Lunziko_LLM`** (main @ ae990f6). Les deux en synchro. **Toutes les améliorations 2026-08-14→16 poussées** (tests+CI, A-4b tool-calling, A-7 MCP, Couche de Contexte A-14→A-18, LAIA AI-CORE + App Requirements + Evaluation + Brain-to-Brain + Safety, A-11 Code Execution sûr, connexion Graphics Engine REST + délégation, connecteurs RAG, SDKs multiplateformes, déploiement Docker + image publiée sur Docker Hub). **SESSION 2026-08-22 (main @ `1d5985e`) : +4 modules → 33 modules, 131 tests verts.** Livrés : **Voix V-3 (traduction MT)** = `/v1/voice/translate` réel (MADLAD-400 on-device OU repli LLM via Provider Manager) + **pipeline `/v1/voice/speak` (interprète STT→MT→TTS)** ; **Voix V-4 (packs téléchargeables)** = téléchargement RÉEL des modèles par composant (voix Piper propre à la langue + Whisper/MADLAD partagés multilingues), `/v1/voice/packs/{id}/{plan,install}` — **prouvé en réel** (voix fr 63 Mo téléchargée+enregistrée) ; **`learning/` mémoire persistante à apprentissage continu** (« apprend toujours, n'oublie jamais » : append-only chiffré, renforcement type répétition espacée, consolidation non destructive, rappel pondéré, archive = tombstone rappelable, **aucune suppression dure**) ; **`autonomy/` personnalité résiliente** (boucle ReAct « ne renonce jamais » : consulte la LTM avant d'agir, journalise échecs+succès, adapte, épuisement→problème ouvert à retenter) ; **`models/` Model Registry** (catalogue des modèles 2026 mappés aux Brains + gouvernance licences : **Qwen 3.8-Max** branché comme provider OpenAI-compat, **MiniMax H3**/Wan 3.0/Wan2.2-Animate/**Nano Banana**/**CrisperWhisper 2.0**/**Gemini Robotics 2**/Prisme.ai en référence/restreint ; Brain `robotics` ajouté) ; **`codeintel/` Lunziko Code Intelligence** (Code Brain élargi : 60+ langages « tous les langages », indexation+recherche sémantique de code, architecture, dépendances, symboles, contexte des 11 projets écosystème ; exposé REST + outils MCP `code_*` + endpoint OpenAI-compat + **CLI `scripts/lzcode.py`** ; `CODE_INTELLIGENCE_INTEGRATIONS.md` = recettes PowerShell/VS Code/Xcode/Visual Studio/Sublime/JetBrains/Android Studio/Docker/MS SQL/PostgreSQL/GitHub/Antigravity). **SUITE 2026-08-22 (main @ `07d397a`) : 35 modules, 163 tests.** (a) **Écriture contrôlée de fichiers** par Code Intelligence (`SafeEditor` : write/edit/delete dry-run+confirm+sandbox de chemin+zones protégées+sauvegarde réversible ; `GitIntelligence` : status/diff/log + checkpoint/commit sous confirm, jamais push/reset --hard) + outils MCP `code_write`/`code_edit` ; `/v1/code-intelligence/{write,edit,delete,restore,backups,git/*}`. (b) **Voix V-5** : profils de rendu des 10 voix (dont 4 ENFANTS approximées) + enregistrement de voix fine-tunées ln/sw (`.onnx` local par voice_id+lang) + prep de fine-tuning (datasets TTS/STT). (c) **`generation/` Brains génératifs multimédias** (image/vidéo/audio/3D) : dispatch réel si backend branché (Graphics Engine/ComfyUI/Fal/Replicate/diffusers GPU), sinon `deferred` explicite + modèles candidats (MiniMax H3/Wan/Nano Banana) ; Brains passent 'declared'→'active' quand un backend est là. (d) **`reasoning/` raisonnement avancé** : catalogue + implémentations clean-room des logiques OSS (CoT, self-consistency, tree-of-thoughts, reflexion, plan-and-solve, step-back, ReAct, debate) provider-agnostiques → **améliore le raisonnement ET renforce l'indépendance** (tourne sur LLM natif/Ollama). (e) **OLLAMA branché & validé 100 % local** (auto-test 10/10 : chat qwen2.5:7b/glm4/deepseek-r1, embeddings nomic dim 768, RAG bout-en-bout, code, tool-calling ; config `AE_LOCAL_MODEL`) → l'AI Engine tourne SANS aucune IA externe. (f) **Image Docker `jaisebukadilu/lunziko-ai-engine:0.2.0`+`latest` rafraîchie + publiée** (digest `47a3597…`). (g) **`lunziko-llm` montée en charge** : presets tiny/small/base/large (0.82M→1.57B) + config distribuée DDP/FSDP + `SCALING.md` (dépôt Lunziko_LLM main @ `fd76b25`).
- **Fonctions exhaustives (endpoints) :**
  - **Chat / génération :** `/v1/chat` (provider manager httpx autonome + fallback).
  - **LLM natif Lunziko (paquet `lunziko-llm/`) :** architecture Transformer moderne **from scratch** (NumPy clean-room : RoPE, RMSNorm, **GQA**, SwiGLU, poids liés + micro-autograd gradient-checké + tokenizer BPE + entraînement Adam + génération). Entraîné offline (modèle jouet), branché comme **provider `lunziko`** (100 % local, sans clé). Montée en charge documentée (PyTorch/GPU, MoE, distribué). Inspiration conceptuelle des LLM ouverts Apache-2.0, aucun code copié.
  - **Embeddings & RAG :** `/v1/embed`, `/v1/rag/*` (repli hash **hors-ligne**).
  - **Mémoire :** `/v1/memory/*` (chiffrée **AES-256-GCM**).
  - **Knowledge graph :** `/v1/knowledge/*` (auto-linking).
  - **Agents :** `/v1/agent/run`.
  - **Workflows :** `/v1/workflow/*` (runs persistés).
  - **Code :** `/v1/code/*` (modèles locaux Ollama).
  - **Compatibilité :** **endpoints OpenAI-compatibles** `/v1/chat/completions`, `/v1/embeddings`, `/v1/models` (drop-in Open WebUI/LocalAI/Continue, auth Bearer/X-API-Key).
  - **Écosystème (A-12) :** `/v1/ecosystem/{sync,apps,apps/{slug},search,status}` — **ingère CE registre au démarrage** (règle §0.4 « analyse au lancement » **implémentée en code** : parseur du roster §1 → index StoragePort+VectorPort) pour **connaître toutes les apps Lunziko et leurs fonctions**, recherche sémantique d'applications (repli hash hors-ligne), **injection automatique dans le contexte des agents**. Découverte auto du fichier ou `AE_REGISTRY_PATH`.
  - **Système neuronal :** `/v1/neural/{status,backends,route,train}` — couche d'abstraction au-dessus des **bibliothèques neuronales** (backend **NumPy natif** + adaptateurs OPTIONNELS PyTorch/TensorFlow/Keras/JAX/**scikit-learn**/**Transformers**, import paresseux jamais requis pour démarrer). Capacité livrée : **routeur d'intention HYBRIDE** (classifieur neuronal embeddings-L2→softmax **fusionné** au signal lexical d'une taxonomie partagée à racines normalisées) qui améliore la réflexion de l'AgentEngine (généralise aux formulations sans mot-clé ; repli mots-clés garanti ; mesuré 12/12 vs 1/12 mots-clés bruts). Bibliothèques importées comme dépendances (licences permissives BSD/Apache), aucun code copié (⚠️ OpenNN LGPL non embarqué).
  - **Assistant d'application (intégrable à TOUTES les apps) :** `/v1/assistant/{app}/{scope,ask,team,agents,ui-contract,sessions}` + **WebSocket** `/v1/assistant/{app}/ws` — assistant **scopé à la zone de compétence** de chaque app (fonctions tirées de CE registre) qui assiste/corrige/agit dans son périmètre et **redirige hors périmètre** (garde de scope). **Jusqu'à 5 agents par application** (plafond appliqué) pour fluidifier les tâches. **Connexion prête pour une future interface visuelle** : WebSocket (événements ready/answer/error) + contrat UI (actions rapides, agents, points de connexion) + sessions persistées. → toute app Lunziko peut embarquer son assistant IA.
  - **Couche de Contexte Unifié (A-14→A-16) :** `/v1/context/assemble`, `/v1/appstate`, `/v1/profile/{user}`(+`/habits`) — contexte IA d'application temps réel : profil & habitudes (dérivées de l'activité ; identité/RBAC = Platform consommée), état applicatif live éphémère (écran/brouillon/erreur, TTL), assembleur unifiant profil+habitudes+activité+état+connaissance+app écosystème (+ temporel/spatial) → bloc `system` injectable.
  - **Feedback (A-18) & Catalogue de schémas (A-17) :** `/v1/feedback{,/stats,/corrections}` (retours up/down + corrections → satisfaction + few-shot pour affiner les réponses) ; `/v1/catalog/{register,schemas,resolve}` (les apps publient leurs schémas de données ; résolution sémantique du schéma pertinent).
  - **LAIA AI-CORE (couche méta d'orchestration) :** `/v1/brains{,/{id},/resolve,/register,/{id}/capabilities}`, `/v1/engines{,/{id}}`, `/v1/orchestrator/{plan,run}`, `/v1/blackboard/tasks{,/{id}}`, `/v1/validate{,/types}` — **Brain Registry** (16 cerveaux : text/reasoning/code/data/research/document/ui_ux/language actifs + vision/image/video/audio/music/voice/3d/cad déclarés), **Engine Registry** (mappe les 23 modules existants), **AI Orchestrator** (intent→contexte→décomposition→Brains→plan→exécution→validation), **AI Blackboard** (état de tâche partagé), **Task Intelligence** (décomposition), **Validation Engine** (checks par type), **Evaluation Engine** (`/v1/evaluate`, score/qualité) et **App Requirements** (`GET/PUT /v1/apps/{app}/requirements` : chaque app déclare ses besoins Brains/Engines/services — VidiaPub→image/video/vision, CAD→cad/3d, BI→data/research… — croisés avec CE registre et injectés dans le plan) et **collaboration Brain-to-Brain** (pipeline séquentiel, chaque cerveau consomme les sorties des précédents via le Blackboard). Construit AU-DESSUS de l'existant.
  - **Search Engine web :** `/v1/search{,/status}` — recherche web **sans clé** (DuckDuckGo par défaut) + **Google Custom Search** optionnel (`AE_GOOGLE_API_KEY`+`AE_GOOGLE_CSE_ID`) ; résultats {title,url,snippet} ; alimente le Research Brain + l'outil `web_search`. Validé en réel.
  - **Safety Engine (garde-fous LAIA) :** `/v1/safety/{check,redact}` — filtrage entrée/sortie : redaction PII (e-mails/téléphones/IBAN/cartes avec validation Luhn), détection d'injection de prompt, modération heuristique. Complète Validation + Evaluation par la dimension sécurité.
  - **Connecteurs RAG (bases de connaissances unifiées) :** `/v1/connectors/{types,ingest,namespaces,search}` — ingère documents/chat/e-mails/fichiers/notes dans le RAG (métadonnées de source) + **recherche unifiée cross-namespace** (fusion par score, attribution de source). Au-dessus du module `rag`.
  - **Code Execution Engine (A-11) :** `/v1/code-exec/{status,eval,run}` — Niveau 0 = safe-eval AST restreint (sûr, toujours ON), Niveau 1 = sandbox subprocess isolé (timeout/env minimal/cwd temp) **désactivé par défaut** (opt-in `AE_CODE_EXEC_ENABLED`, isolation OS requise pour code non fiable). Boucle `generate→run→fix` du Code Brain. Cf. `CODE_EXECUTION_SANDBOX.md`.
  - **Branchement Graphics Engine :** `/v1/graphics/{status,ping,brains,call}` — adaptateur **client REST** vers le Lunziko Graphics Engine (dépôt séparé `Desktop\Lunziko\Lunziko Graphics Engine`, serveur FastAPI 127.0.0.1:8000 `X-API-Key`, 93 endpoints/22 agents, NON modifié) ; mapping Brain→groupes d'endpoints (image→imaging/vector/pdf…, 3d→mesh/render/asset/cad/sketch…) ; quand `AE_GRAPHICS_ENGINE_URL` branché, les **Brains image/vision/video/3d/cad passent de « declared » à « active »**. **Connexion validée en réel** (health/agents/capabilities OK, 22 agents).
  - **Action Registry :** `/v1/actions/{register,invoke}` + `GET /v1/actions` — les apps **déclarent leurs actions exécutables** (schéma d'arguments, requires_confirmation, executor) ; l'AI Engine valide et produit une **instruction d'action structurée** (deep_link `lunziko://{app}/action/{action}`) exécutée par l'app hôte. Complète handoff (navigation/fichiers) + tools (capacités IA).
  - **Automatisation (A-10) :** `/v1/automation/{flows,flows/{name}/run,runs}` — moteur de flux par nœuds (clean-room, inspiré n8n, réimplémenté from scratch) : chaque nœud appelle un outil du registre (A-4b), args référençant `$input.x` ou `$node.champ` ; flux et exécutions persistés.
  - **MCP (A-7) :** `POST /mcp` (JSON-RPC 2.0 : initialize/tools.list/tools.call), `GET /mcp`, `POST /v1/mcp/import` — **serveur MCP** exposant les outils de l'AI Engine (consommable par Claude Desktop/Cline/Continue) + **client MCP** qui importe les outils d'un serveur externe dans le registre local. Standard ouvert MIT, clean-room.
  - **Tool-calling natif (A-4b) :** `/v1/tools` (registre), `/v1/tools/run` (exécution directe), `/v1/agent/act` (boucle) — les agents **agissent** : outils = nom+description+schéma JSON+handler ; **outils intégrés** sur les capacités existantes (`ecosystem_search`, `handoff_open_with`, `data_clean_text`, `ml_predict`, `activity_timeline`) ; boucle provider-agnostique (modèle→demande d'outil→exécution→re-boucle→réponse) ; tool-calling implémenté pour **Claude** et providers **OpenAI-compatibles**.
  - **Handoff inter-applications :** `/v1/handoff/{redirect,open-with,transfer,file-types}` — depuis une app, selon la situation : **rediriger** l'utilisateur vers l'app Lunziko compétente pour poursuivre sa tâche, **transférer** un fichier/dossier vers une autre app, ou l'**ouvrir dans l'app la plus adaptée** (résolution par type de fichier `.xlsx→DociaPub/MySheet`, `.dwg→CAD`, `.png→VidiaPub/Photo`, `.ifc→CAD/BIM`… sinon recherche sémantique dans CE registre). Produit des **actions structurées** (deep-link `lunziko://{app}/…`, executor = app hôte / HUB / Platform) exécutées par l'hôte ; intégré à l'assistant (hors périmètre → redirection attachée). **Rôle HUB (lanceur) + Platform (transport)** pour l'exécution réelle.
  - **Données (pilier « matière première ») :** `/v1/data/{profile,clean,clean-text,prepare-rag,prepare-corpus,prepare-training}` — profilage (types/nuls/distincts), nettoyage tabulaire (trim, coercition de types, valeurs manquantes, **déduplication**, lignes vides) + nettoyage de corpus texte, puis préparation vers RAG / corpus du LLM natif / table d'entraînement ML. Pur Python offline, clean-room (inspiration OpenRefine/KNIME).
  - **Apprentissage ML & inférence (pilier « algorithmes/modèles ») :** `/v1/neural/ml/{train,predict,models}` (classifieur supervisé **appris depuis exemples**, embeddings→softmax NumPy ou scikit-learn, **persisté**) ; `/v1/neural/inference` (catalogue des moteurs d'inférence locaux **Ollama/llama.cpp/vLLM/LM Studio/Triton** + provider natif `lunziko`, consommés via le Provider Manager OpenAI-compat).
  - **Activité (A-13) :** `/v1/activity/{log,log-batch,timeline,search,summary}` + `DELETE` — **journal des actions utilisateur** (les apps publient ce que fait l'utilisateur) = 1ʳᵉ brique de la **Couche de Contexte Unifié** (`CONTEXT_LAYER_ARCHITECTURE.md`, A-13→A-18) ; timeline + recherche sémantique + résumé, champ libre **chiffré** (AES-256-GCM), injecté dans le contexte des agents. **À venir : appstate (A-16), profil/RBAC consommé de Platform (A-14), assembleur de contexte (A-15), connecteurs RAG, catalogue de schémas (A-17), feedback (A-18), Action Registry (function calling).**
  - **Voix (V-1→V-4 RÉELLE) :** **TTS = Piper** (`/v1/voice/tts` → `audio/wav`, voix `.onnx` ex. fr_FR-siwis-medium 63 Mo, extra `voice-tts`, `AE_TTS_VOICES_DIR`) **+ STT = Whisper via faster-whisper** (`/v1/voice/stt`, modèle `base` 145 Mo `AE_STT_MODEL`, extra `voice-stt`) **+ MT V-3 = traduction** (`/v1/voice/translate`, MADLAD-400 on-device OU repli LLM via Provider Manager, extra `voice-mt`, `AE_MT_BACKEND`) **+ pipeline V-3 `/v1/voice/speak` (interprète STT→MT→TTS)** **+ packs V-4 téléchargeables** (`/v1/voice/packs/{id}/{plan,install}` : téléchargement réel par composant — voix Piper propre + Whisper/MADLAD partagés multilingues ; registre des composants installés). **Round-trip TTS→STT validé + download voix fr prouvé en réel** ; `/tts/status`+`/stt/status`+`/translate/status`. **18 langues** (fr/en/es/de/it/pt/ru/el/he/ar/hi/bn/ur/id/zh/ja/sw/ln).
  - **Génération multimédia (Brains génératifs image/vidéo/audio/3D) :** `/v1/generate/{status,backends,models/{kind},image,video,audio,music,3d}` — dispatche vers un backend génératif branché (Graphics Engine / ComfyUI / Fal / Replicate / diffusers GPU) ; sans backend, réponse `deferred` explicite (backends candidats + modèles du catalogue **MiniMax H3/Wan/Nano Banana** + note GPU/licence) — **jamais de génération simulée**. Les Brains image/video/audio/music/3d passent « declared »→« active » dès qu'un backend est disponible.
  - **Raisonnement avancé (logiques des IA open source, clean-room) :** `/v1/reasoning/{strategies,strategies/{id},reason}` — catalogue + implémentations **provider-agnostiques** des techniques OSS : **Chain-of-Thought, Self-Consistency (vote majoritaire), Tree-of-Thoughts, Reflexion (auto-critique+révision), Plan-and-Solve, Least-to-Most, Step-Back, ReAct, Multi-Agent Debate** (concepts publics réimplémentés, aucun code copié). Sélection automatique de stratégie. **Améliore le raisonnement ET renforce l'autonomie** : tourne sur le LLM natif `lunziko`, Ollama local ou tout provider (validé CoT en local Ollama).
  - **Mémoire persistante à apprentissage continu (« apprend toujours, n'oublie jamais ») :** `/v1/learning/{remember,observe,recall,reinforce,consolidate,timeline,archive,stats}` — mémoire long-terme **append-only** chiffrée (AES-GCM) au-dessus des ports : réapprendre un fait proche le **renforce** (répétition espacée) au lieu de le dupliquer ; **consolidation non destructive** (quasi-doublons liés à un canonique, jamais effacés) ; **rappel pondéré** (similarité × renforcement × importance) ; `archive` = oubli **soft** (tombstone rappelable via `include_archived`), **aucune suppression dure**. Complète la mémoire courte (`/v1/memory`), l'activité (`/v1/activity`) et le knowledge graph.
  - **Autonomie & résilience (personnalité « ne renonce jamais ») :** `/v1/autonomy/{solve,persona}` — boucle agentique **ReAct** (Réflexion→Action→Observation) qui **consulte la mémoire long-terme AVANT d'agir** (ne répète pas une erreur passée), **journalise échecs ET succès** dans la LTM (auto-évolution), **adapte** sa stratégie après échec et **n'abandonne jamais** (épuisement → PROBLÈME OUVERT mémorisé à retenter, jamais de « je ne sais pas, demande à l'utilisateur »). System prompt de détermination ; s'appuie sur Tools/Search/Code-Exec existants.
  - **Model Registry (catalogue des modèles d'IA + gouvernance licences) :** `/v1/model-catalog/{,stats,usable,restricted,code-tools,by-brain/{brain},{id}}` — catalogue des modèles concrets (dont la vague 2026 : **Qwen 3.8-Max** utilisable via provider OpenAI-compat ; **MiniMax H3**, **Wan 3.0**, **Wan2.2-Animate-14B**, **Nano Banana**, **CrisperWhisper 2.0**, **Gemini Robotics 2**, **Prisme.ai** en référence/restreint) **mappés aux Brains LAIA**, avec licence, poids ouverts, compat API, keyless et **restrictions juridiques** (ex. MiniMax H3 exclut UE/US/UK/Corée du Sud). Discipline clean-room : backends/références, aucun code copié. Ajoute le Brain `robotics`.
  - **Lunziko Code Intelligence (Code Brain élargi, « maîtrise tous les langages ») :** `/v1/code-intelligence/{languages,detect,index,search,understand,dependencies,symbols,projects,project/{p}}` — **catalogue 60+ langages** (généralistes/systèmes/web/mobile/data/fonctionnels/shells/requêtes/HDL/shaders/legacy) + détection par extension ; **indexation + recherche sémantique de code** ; analyse d'**architecture** (langages, points d'entrée, manifestes) ; extraction de **dépendances** (npm/pip/cargo/go/maven/spm/pub…) ; **symboles** multi-langages ; **contexte des 11 projets écosystème** (croisé avec CE registre). **Utilisable depuis tous les outils** via REST + **outils MCP `code_*`** + endpoint OpenAI-compat + **CLI `scripts/lzcode.py`** — recettes PowerShell/VS Code/Xcode/Visual Studio/Sublime/JetBrains/Android Studio/Docker/MS SQL/PostgreSQL/GitHub/Antigravity (`CODE_INTELLIGENCE_INTEGRATIONS.md`).
  - **Persistance :** ports SQLite/vector/blob ; **adaptateur Postgres/pgvector** (couplage optionnel Platform).
  - **SDKs multiplateformes (A-9) :** clients du gateway en **TypeScript** (`sdk/typescript/`), **Swift** (`sdk/swift/`, async/await macOS/iOS), **Kotlin** (`sdk/kotlin/`, JVM/Android), **Dart** (`sdk/dart/`, Flutter) — surface commune (chat/embed/rag/mémoire/agent/act-outils/écosystème/activité/contexte/assistant/handoff/neural/data/automation + get/post génériques). SDK Dart validé (`dart analyze` OK).
  - **À venir :** V-5 fine-tune voix lingála/swahili + voix enfants ; activation effective des Brains génératifs multimédias (modèles image/vidéo/3D dédiés + Graphics Engine) ; entraînement LLM natif à grande échelle (GPU) ; écriture/modification contrôlée de fichiers par Code Intelligence (avec garde-fous avant toute action destructive).
- **Expose :** raisonnement, RAG, agents, workflows, mémoire, code, voix, endpoints OpenAI/MCP. **Consomme :** **manifestes & fonctions de toutes les apps (ce registre)** pour accompagner/résoudre les tâches utilisateur.

## Toutes les applications de l'écosystème (roster §1)

| Application | Catégorie | Expose (résumé) |
|---|---|---|
| **Lunziko Platform** | socle central & **autorité de licences** de l'écosystème. | identité, licences, IA gateway, API, bus, signature, registre apps/logos |
| **Lunziko HUB** | distribution & gouvernance (app-store interne, style Creative Cloud) de l'écosystème (5 suites). | distribution, MàJ, catalogue téléchargeable, relations |
| **Lunziko AI Engine** ⭐ | IA autonome (indépendante de Platform ; Platform = client optionnel). Objectif : **accompagner l'utilisateur** sur toutes les applications. **Architecture = LAIA (Lunziko AI Intelligence Architecture, Multi-Brain/Multi-Engine)** : un Orchestrator coordonne des **Brains** (cerveaux spécialisés), des **Engines** (les modules) et des **Tools** — construit AU-DESSUS de l'existant (cf. `LAIA_ARCHITECTURE.md`). | raisonnement, RAG, agents, workflows, mémoire, code, voix, endpoints OpenAI/MCP |
| **Lunziko Graphics Engine (LGE)** | moteur graphique / rendu (JSON-RPC, architecture à **32 agents** — Amélioration 2 : SceneOptimizer + MaterialCatalog + ClashDetection + RuleChecker + NURBS + GPUCompute + NVIDIA RTX + Physics + AI-Render + **O3DE Bridge** livrés). | rendus haute qualité (graphiques/visuels), imaging, vector, PDF, CAD/BIM/sketch. **Exposera (roadmap V2) :** NURBS, GPUCompute, ClashDetection, RuleChecker, AI-Render, Physics, MaterialCatalog, SceneOptimizer. **Exposera (Amélioration 2 « moteur universel ») :** backends GPU (DX12/Metal/Vulkan/WebGPU), 2D/vidéo/audio temps réel, chroma/segmentation IA, texte pro, vectoriel avancé, path tracing PBR/HDR, simulation multiphysique (FEA/thermique/fluides), IA graphique (NL→ops, croquis→CAO, plan→BIM), SceneOptimizer + cache GPU partagé, multi-sorties 16:9+9:16, formats CAO/BIM (STEP/IGES/IFC/BCF/DWG*) + vectoriel (SVG/EPS/PDF) + raster pro (HDR/ICC/LUT) |
| **Lunziko Design System (LDS)** | plateforme de conception unifiée de **toute** la suite Lunziko. | tokens & composants (build-time, aucun appel runtime) |
| **Lunziko One** | suite **ERP** métier modulaire IA-native (FR + CH). Adossée à Platform. | données métier + événements (`finance.entry.posted`, `payroll.run.closed`, `sales.order.confirmed`…) via **Lunziko Data API** |
| **Lunziko BI** | **Business Intelligence / Data Analytics / Reporting** — couche Data/Analytics commune de l'écosystème. | analytics, dashboards, graphiques liés, API BI, embedding |
| **DociaPub (Lunziko DociaPub)** | suite **documentaire / bureautique** (cible macOS + Windows, moteur OOXML/ODF ; legacy MyOfficeSuite Swift 43k LOC comme référence). | génération/édition/rendu de documents ; MySheet/MyData = sources ; MyWord/MySlides/MyMail/MyPublish = cibles |
| **Lunziko VidiaPub** | suite **créative** (macOS + Windows ; nom interne « Lunziko »). | création graphique/vidéo/audio/PDF/logo (+ **manifeste** aux 5 agrégateurs, règle §0.2, à brancher) |
| **Lunziko CAD** | **CAO / DAO** (Architecture + CAO mécanique). | modèles CAO/DAO |
| **Lunziko Yekoli** | plateforme d'**apprentissage des langues**. | contenus & progression d'apprentissage |
| **Lunziko Sentinel** | **cybersécurité souveraine** — antivirus / EPP / EDR / XDR multi-plateforme (code 100 % propriétaire, aucune licence tierce). Console de sécurité web = **Sentinel Platform**. | état de protection des endpoints, alertes/incidents, IOCs, rapports de conformité, actions distantes (Action Registry), événements `sentinel.threat.detected`/`sentinel.incident.opened`/`sentinel.device.isolated`… (bus Platform + webhooks) |
| **Lunziko Live Stream** | **pôle Live Media** de l'écosystème — plateforme de **production, réalisation, enregistrement et diffusion vidéo en direct multiplateforme** (studio live + régie multicaméra + multistream + serveur auto-hébergeable). Cible : créateurs, entreprises, événements, églises, gaming, webinaires, podcasts. | état des streams/destinations, scènes/sources, enregistrements, chat unifié, player web/embed, événements `livestream.stream.started`/`livestream.destination.connected`/`livestream.recording.saved`/`livestream.clip.generated`… (bus Platform + webhooks) ; actions distantes (Action Registry) |

## Détail des fonctions par application (extrait du registre)

### Lunziko Platform

- **Catégorie :** socle central & **autorité de licences** de l'écosystème.
- **Statut :** monorepo bun+turbo déployé (Vercel) — `apps/web` (Next.js 14, `lunziko-platform.vercel.app`) + `server/api` (Hono, `lunziko-api.vercel.app`) + `packages/sdk-typescript` (12 modules) + `server/gaming-engine` (C++) + microservices 3D-python / piper-tts. Supabase `xtvfkyfjsnrzmtgnzicc`. Chantier licences hybride (branche `feature/licensing-authority`).
- **Fonctions exhaustives :**
  - **Identité & accès :** SSO/Auth **OIDC/OAuth2**, JWT, **RBAC**, **multi-tenant** (multi-sociétés), gestion utilisateurs/rôles/scopes.
  - **Licences & entitlements (autorité unique) :** émission, révocation, **JWT de licence signé** vérifiable hors-ligne, entitlements par app/module/**service** (sdk, agent_deployment), **provisionnement** (membres entreprise, bénéficiaires particuliers), **allocation par app**, **partage famille**, **download-gating** (piloté vers HUB), consoles **Staff/Support**, admin provisioning.
  - **Facturation :** Subscription, Billing, paliers d'abonnement.
  - **IA :** **Gateway IA** multi-providers (claude/openai/gemini/mistral/deepseek + **auto-fallback**), **ThinkingEngine**, **SkillsRegistry**, **PersonaManager**.
  - **API & événementiel :** **API Gateway** REST/GraphQL, **bus de messages** (backbone interne, ordre/rejeu/back-pressure) + **webhooks** (façade externe).
  - **3D & jeu :** **3D Pipeline** (Blender/Unity/Unreal/Maya + GPU OptiX/CUDA/Arc), **Gaming Engine v2** (6 agents IA).
  - **Transverse :** sécurité (chiffrement transit/repos, audit/traçabilité), notifications, reporting, moteur de recherche global, workflows configurables, **service « Trust & Signature »** (signature qualifiée eIDAS/ZertES, horodatage, coffre-fort), **registre des apps & logos**.
- **Expose :** identité, licences, IA gateway, API, bus, signature, registre apps/logos. **Consomme :** — (socle ; agrège les manifestes de toutes les apps).

### Lunziko HUB

- **Catégorie :** distribution & gouvernance (app-store interne, style Creative Cloud) de l'écosystème (5 suites).
- **Statut :** CDC v2.0 + charte « Dark Studio » (#131313 / **#007AFF**, Inter+JetBrains Mono) + socle UI `client/` (Vite+React+TS+Tailwind), Dashboard Unified Manager v2.0. Reco stack Tauri/Rust + PostgreSQL Francfort.
- **Fonctions exhaustives :**
  - **Catalogue :** liste des applications **téléchargeables**, fiches produit, logos, catégories.
  - **Distribution :** téléchargement, **installation**, **mise à jour** des apps & modules ; **canaux de release** (stable/beta/…) ; delta updates.
  - **Gouvernance des droits :** **download-gating** selon licences (activation/désactivation d'apps & de modules), **y compris hors-ligne** ; se réfère **en priorité à Platform** (licences consommées hors-ligne).
  - **Relations :** **affichage des relations** entre apps (Applications compatibles + Services : Platform/AI/Data Layer).
  - **Lancement :** lanceur des applications de l'écosystème (Unified Manager).
- **Expose :** distribution, MàJ, catalogue téléchargeable, relations. **Consomme :** **Platform en priorité** (licences), ce registre.

### Lunziko AI Engine

- **Catégorie :** IA autonome (indépendante de Platform ; Platform = client optionnel). Objectif : **accompagner l'utilisateur** sur toutes les applications. **Architecture = LAIA (Lunziko AI Intelligence Architecture, Multi-Brain/Multi-Engine)** : un Orchestrator coordonne des **Brains** (cerveaux spécialisés), des **Engines** (les modules) et des **Tools** — construit AU-DESSUS de l'existant (cf. `LAIA_ARCHITECTURE.md`).
- **Statut :** code `lunziko-ai-engine/` (FastAPI, cœur Python, persistance enfichable locale). Modules **A-0→A-6 + A-8 + A-12 + A-13 + neural + data + assistant + handoff + tools (A-4b) + mcp (A-7) + context (A-14→A-16) + feedback (A-18) + catalog (A-17) + automation (A-10) + actions + **orchestrator (LAIA AI-CORE)** + **codeexec (A-11)** + **graphics (branchement)** + **connectors (RAG unifié)** + **safety (garde-fous)** + **search (web keyless)** + **voix V-1 réelle (TTS Piper + STT Whisper)** livrés** (29 modules ; Couche de Contexte A-14→A-18 COMPLÈTE ; architecture LAIA Multi-Brain/Multi-Engine + App Requirements + Evaluation + Brain-to-Brain + Code Execution sûr + Graphics Engine connecté (REST, validé) + connecteurs RAG + Safety Engine + **orchestrateur→Graphics délégué** (validé LIVE : 3d→delegated, 22 agents) + **Search web DuckDuckGo sans clé** + **Voix V-1 TTS+STT réels** (round-trip validé)). Suite pytest offline (**95 tests**) + CI GitHub Actions. **SDKs A-9 : TS + Swift + Kotlin + Dart** (Dart validé `dart analyze`). **🐳 DÉPLOIEMENT DOCKER (2026-08-16) : image `jaisebukadilu/lunziko-ai-engine:latest`+`:0.1.0` BUILDÉE + smoke-test `/health` OK (28 modules actifs) + PUBLIÉE sur Docker Hub** (Dockerfile+docker-compose+DEPLOY.md ; `docker pull jaisebukadilu/lunziko-ai-engine`). py_compile OK + tests offline (E2E écosystème : sync v1.2, 11 apps ; E2E activité : chiffrement AES-GCM ; E2E neural : routeur d'intention **HYBRIDE 12/12** vs 1/12 mots-clés bruts). Spec **Couche de Contexte Unifié** `CONTEXT_LAYER_ARCHITECTURE.md`. **+ LLM natif from scratch `lunziko-llm/`** (NumPy gradient-checké + **backend PyTorch GPU-ready** — même archi RoPE/RMSNorm/GQA/SwiGLU/poids liés, entraîné offline loss 6.0→0.11) branché comme provider `lunziko` ; dépôt `github.com/jaisebukadilu/Lunziko_LLM`. **SOUS GIT + GITHUB (2026-08-14)** : 2 dépôts poussés — `lunziko-ai-engine/` (83 fichiers) sur **`github.com/jaisebukadilu/Lunziko_Ai_Engine`** (main @ 43e0967, 17 commits, ~136 fichiers py) + `lunziko-llm/` (13 fichiers) sur **`github.com/jaisebukadilu/Lunziko_LLM`** (main @ ae990f6). Les deux en synchro. **Toutes les améliorations 2026-08-14→16 poussées** (tests+CI, A-4b tool-calling, A-7 MCP, Couche de Contexte A-14→A-18, LAIA AI-CORE + App Requirements + Evaluation + Brain-to-Brain + Safety, A-11 Code Execution sûr, connexion Graphics Engine REST + délégation, connecteurs RAG, SDKs multiplateformes, déploiement Docker + image publiée sur Docker Hub). **SESSION 2026-08-22 (main @ `1d5985e`) : +4 modules → 33 modules, 131 tests verts.** Livrés : **Voix V-3 (traduction MT)** = `/v1/voice/translate` réel (MADLAD-400 on-device OU repli LLM via Provider Manager) + **pipeline `/v1/voice/speak` (interprète STT→MT→TTS)** ; **Voix V-4 (packs téléchargeables)** = téléchargement RÉEL des modèles par composant (voix Piper propre à la langue + Whisper/MADLAD partagés multilingues), `/v1/voice/packs/{id}/{plan,install}` — **prouvé en réel** (voix fr 63 Mo téléchargée+enregistrée) ; **`learning/` mémoire persistante à apprentissage continu** (« apprend toujours, n'oublie jamais » : append-only chiffré, renforcement type répétition espacée, consolidation non destructive, rappel pondéré, archive = tombstone rappelable, **aucune suppression dure**) ; **`autonomy/` personnalité résiliente** (boucle ReAct « ne renonce jamais » : consulte la LTM avant d'agir, journalise échecs+succès, adapte, épuisement→problème ouvert à retenter) ; **`models/` Model Registry** (catalogue des modèles 2026 mappés aux Brains + gouvernance licences : **Qwen 3.8-Max** branché comme provider OpenAI-compat, **MiniMax H3**/Wan 3.0/Wan2.2-Animate/**Nano Banana**/**CrisperWhisper 2.0**/**Gemini Robotics 2**/Prisme.ai en référence/restreint ; Brain `robotics` ajouté) ; **`codeintel/` Lunziko Code Intelligence** (Code Brain élargi : 60+ langages « tous les langages », indexation+recherche sémantique de code, architecture, dépendances, symboles, contexte des 11 projets écosystème ; exposé REST + outils MCP `code_*` + endpoint OpenAI-compat + **CLI `scripts/lzcode.py`** ; `CODE_INTELLIGENCE_INTEGRATIONS.md` = recettes PowerShell/VS Code/Xcode/Visual Studio/Sublime/JetBrains/Android Studio/Docker/MS SQL/PostgreSQL/GitHub/Antigravity). **SUITE 2026-08-22 (main @ `07d397a`) : 35 modules, 163 tests.** (a) **Écriture contrôlée de fichiers** par Code Intelligence (`SafeEditor` : write/edit/delete dry-run+confirm+sandbox de chemin+zones protégées+sauvegarde réversible ; `GitIntelligence` : status/diff/log + checkpoint/commit sous confirm, jamais push/reset --hard) + outils MCP `code_write`/`code_edit` ; `/v1/code-intelligence/{write,edit,delete,restore,backups,git/*}`. (b) **Voix V-5** : profils de rendu des 10 voix (dont 4 ENFANTS approximées) + enregistrement de voix fine-tunées ln/sw (`.onnx` local par voice_id+lang) + prep de fine-tuning (datasets TTS/STT). (c) **`generation/` Brains génératifs multimédias** (image/vidéo/audio/3D) : dispatch réel si backend branché (Graphics Engine/ComfyUI/Fal/Replicate/diffusers GPU), sinon `deferred` explicite + modèles candidats (MiniMax H3/Wan/Nano Banana) ; Brains passent 'declared'→'active' quand un backend est là. (d) **`reasoning/` raisonnement avancé** : catalogue + implémentations clean-room des logiques OSS (CoT, self-consistency, tree-of-thoughts, reflexion, plan-and-solve, step-back, ReAct, debate) provider-agnostiques → **améliore le raisonnement ET renforce l'indépendance** (tourne sur LLM natif/Ollama). (e) **OLLAMA branché & validé 100 % local** (auto-test 10/10 : chat qwen2.5:7b/glm4/deepseek-r1, embeddings nomic dim 768, RAG bout-en-bout, code, tool-calling ; config `AE_LOCAL_MODEL`) → l'AI Engine tourne SANS aucune IA externe. (f) **Image Docker `jaisebukadilu/lunziko-ai-engine:0.2.0`+`latest` rafraîchie + publiée** (digest `47a3597…`). (g) **`lunziko-llm` montée en charge** : presets tiny/small/base/large (0.82M→1.57B) + config distribuée DDP/FSDP + `SCALING.md` (dépôt Lunziko_LLM main @ `fd76b25`).
- **Fonctions exhaustives (endpoints) :**
  - **Chat / génération :** `/v1/chat` (provider manager httpx autonome + fallback).
  - **LLM natif Lunziko (paquet `lunziko-llm/`) :** architecture Transformer moderne **from scratch** (NumPy clean-room : RoPE, RMSNorm, **GQA**, SwiGLU, poids liés + micro-autograd gradient-checké + tokenizer BPE + entraînement Adam + génération). Entraîné offline (modèle jouet), branché comme **provider `lunziko`** (100 % local, sans clé). Montée en charge documentée (PyTorch/GPU, MoE, distribué). Inspiration conceptuelle des LLM ouverts Apache-2.0, aucun code copié.
  - **Embeddings & RAG :** `/v1/embed`, `/v1/rag/*` (repli hash **hors-ligne**).
  - **Mémoire :** `/v1/memory/*` (chiffrée **AES-256-GCM**).
  - **Knowledge graph :** `/v1/knowledge/*` (auto-linking).
  - **Agents :** `/v1/agent/run`.
  - **Workflows :** `/v1/workflow/*` (runs persistés).
  - **Code :** `/v1/code/*` (modèles locaux Ollama).
  - **Compatibilité :** **endpoints OpenAI-compatibles** `/v1/chat/completions`, `/v1/embeddings`, `/v1/models` (drop-in Open WebUI/LocalAI/Continue, auth Bearer/X-API-Key).
  - **Écosystème (A-12) :** `/v1/ecosystem/{sync,apps,apps/{slug},search,status}` — **ingère CE registre au démarrage** (règle §0.4 « analyse au lancement » **implémentée en code** : parseur du roster §1 → index StoragePort+VectorPort) pour **connaître toutes les apps Lunziko et leurs fonctions**, recherche sémantique d'applications (repli hash hors-ligne), **injection automatique dans le contexte des agents**. Découverte auto du fichier ou `AE_REGISTRY_PATH`.
  - **Système neuronal :** `/v1/neural/{status,backends,route,train}` — couche d'abstraction au-dessus des **bibliothèques neuronales** (backend **NumPy natif** + adaptateurs OPTIONNELS PyTorch/TensorFlow/Keras/JAX/**scikit-learn**/**Transformers**, import paresseux jamais requis pour démarrer). Capacité livrée : **routeur d'intention HYBRIDE** (classifieur neuronal embeddings-L2→softmax **fusionné** au signal lexical d'une taxonomie partagée à racines normalisées) qui améliore la réflexion de l'AgentEngine (généralise aux formulations sans mot-clé ; repli mots-clés garanti ; mesuré 12/12 vs 1/12 mots-clés bruts). Bibliothèques importées comme dépendances (licences permissives BSD/Apache), aucun code copié (⚠️ OpenNN LGPL non embarqué).
  - **Assistant d'application (intégrable à TOUTES les apps) :** `/v1/assistant/{app}/{scope,ask,team,agents,ui-contract,sessions}` + **WebSocket** `/v1/assistant/{app}/ws` — assistant **scopé à la zone de compétence** de chaque app (fonctions tirées de CE registre) qui assiste/corrige/agit dans son périmètre et **redirige hors périmètre** (garde de scope). **Jusqu'à 5 agents par application** (plafond appliqué) pour fluidifier les tâches. **Connexion prête pour une future interface visuelle** : WebSocket (événements ready/answer/error) + contrat UI (actions rapides, agents, points de connexion) + sessions persistées. → toute app Lunziko peut embarquer son assistant IA.
  - **Couche de Contexte Unifié (A-14→A-16) :** `/v1/context/assemble`, `/v1/appstate`, `/v1/profile/{user}`(+`/habits`) — contexte IA d'application temps réel : profil & habitudes (dérivées de l'activité ; identité/RBAC = Platform consommée), état applicatif live éphémère (écran/brouillon/erreur, TTL), assembleur unifiant profil+habitudes+activité+état+connaissance+app écosystème (+ temporel/spatial) → bloc `system` injectable.
  - **Feedback (A-18) & Catalogue de schémas (A-17) :** `/v1/feedback{,/stats,/corrections}` (retours up/down + corrections → satisfaction + few-shot pour affiner les réponses) ; `/v1/catalog/{register,schemas,resolve}` (les apps publient leurs schémas de données ; résolution sémantique du schéma pertinent).
  - **LAIA AI-CORE (couche méta d'orchestration) :** `/v1/brains{,/{id},/resolve,/register,/{id}/capabilities}`, `/v1/engines{,/{id}}`, `/v1/orchestrator/{plan,run}`, `/v1/blackboard/tasks{,/{id}}`, `/v1/validate{,/types}` — **Brain Registry** (16 cerveaux : text/reasoning/code/data/research/document/ui_ux/language actifs + vision/image/video/audio/music/voice/3d/cad déclarés), **Engine Registry** (mappe les 23 modules existants), **AI Orchestrator** (intent→contexte→décomposition→Brains→plan→exécution→validation), **AI Blackboard** (état de tâche partagé), **Task Intelligence** (décomposition), **Validation Engine** (checks par type), **Evaluation Engine** (`/v1/evaluate`, score/qualité) et **App Requirements** (`GET/PUT /v1/apps/{app}/requirements` : chaque app déclare ses besoins Brains/Engines/services — VidiaPub→image/video/vision, CAD→cad/3d, BI→data/research… — croisés avec CE registre et injectés dans le plan) et **collaboration Brain-to-Brain** (pipeline séquentiel, chaque cerveau consomme les sorties des précédents via le Blackboard). Construit AU-DESSUS de l'existant.
  - **Search Engine web :** `/v1/search{,/status}` — recherche web **sans clé** (DuckDuckGo par défaut) + **Google Custom Search** optionnel (`AE_GOOGLE_API_KEY`+`AE_GOOGLE_CSE_ID`) ; résultats {title,url,snippet} ; alimente le Research Brain + l'outil `web_search`. Validé en réel.
  - **Safety Engine (garde-fous LAIA) :** `/v1/safety/{check,redact}` — filtrage entrée/sortie : redaction PII (e-mails/téléphones/IBAN/cartes avec validation Luhn), détection d'injection de prompt, modération heuristique. Complète Validation + Evaluation par la dimension sécurité.
  - **Connecteurs RAG (bases de connaissances unifiées) :** `/v1/connectors/{types,ingest,namespaces,search}` — ingère documents/chat/e-mails/fichiers/notes dans le RAG (métadonnées de source) + **recherche unifiée cross-namespace** (fusion par score, attribution de source). Au-dessus du module `rag`.
  - **Code Execution Engine (A-11) :** `/v1/code-exec/{status,eval,run}` — Niveau 0 = safe-eval AST restreint (sûr, toujours ON), Niveau 1 = sandbox subprocess isolé (timeout/env minimal/cwd temp) **désactivé par défaut** (opt-in `AE_CODE_EXEC_ENABLED`, isolation OS requise pour code non fiable). Boucle `generate→run→fix` du Code Brain. Cf. `CODE_EXECUTION_SANDBOX.md`.
  - **Branchement Graphics Engine :** `/v1/graphics/{status,ping,brains,call}` — adaptateur **client REST** vers le Lunziko Graphics Engine (dépôt séparé `Desktop\Lunziko\Lunziko Graphics Engine`, serveur FastAPI 127.0.0.1:8000 `X-API-Key`, 93 endpoints/22 agents, NON modifié) ; mapping Brain→groupes d'endpoints (image→imaging/vector/pdf…, 3d→mesh/render/asset/cad/sketch…) ; quand `AE_GRAPHICS_ENGINE_URL` branché, les **Brains image/vision/video/3d/cad passent de « declared » à « active »**. **Connexion validée en réel** (health/agents/capabilities OK, 22 agents).
  - **Action Registry :** `/v1/actions/{register,invoke}` + `GET /v1/actions` — les apps **déclarent leurs actions exécutables** (schéma d'arguments, requires_confirmation, executor) ; l'AI Engine valide et produit une **instruction d'action structurée** (deep_link `lunziko://{app}/action/{action}`) exécutée par l'app hôte. Complète handoff (navigation/fichiers) + tools (capacités IA).
  - **Automatisation (A-10) :** `/v1/automation/{flows,flows/{name}/run,runs}` — moteur de flux par nœuds (clean-room, inspiré n8n, réimplémenté from scratch) : chaque nœud appelle un outil du registre (A-4b), args référençant `$input.x` ou `$node.champ` ; flux et exécutions persistés.
  - **MCP (A-7) :** `POST /mcp` (JSON-RPC 2.0 : initialize/tools.list/tools.call), `GET /mcp`, `POST /v1/mcp/import` — **serveur MCP** exposant les outils de l'AI Engine (consommable par Claude Desktop/Cline/Continue) + **client MCP** qui importe les outils d'un serveur externe dans le registre local. Standard ouvert MIT, clean-room.
  - **Tool-calling natif (A-4b) :** `/v1/tools` (registre), `/v1/tools/run` (exécution directe), `/v1/agent/act` (boucle) — les agents **agissent** : outils = nom+description+schéma JSON+handler ; **outils intégrés** sur les capacités existantes (`ecosystem_search`, `handoff_open_with`, `data_clean_text`, `ml_predict`, `activity_timeline`) ; boucle provider-agnostique (modèle→demande d'outil→exécution→re-boucle→réponse) ; tool-calling implémenté pour **Claude** et providers **OpenAI-compatibles**.
  - **Handoff inter-applications :** `/v1/handoff/{redirect,open-with,transfer,file-types}` — depuis une app, selon la situation : **rediriger** l'utilisateur vers l'app Lunziko compétente pour poursuivre sa tâche, **transférer** un fichier/dossier vers une autre app, ou l'**ouvrir dans l'app la plus adaptée** (résolution par type de fichier `.xlsx→DociaPub/MySheet`, `.dwg→CAD`, `.png→VidiaPub/Photo`, `.ifc→CAD/BIM`… sinon recherche sémantique dans CE registre). Produit des **actions structurées** (deep-link `lunziko://{app}/…`, executor = app hôte / HUB / Platform) exécutées par l'hôte ; intégré à l'assistant (hors périmètre → redirection attachée). **Rôle HUB (lanceur) + Platform (transport)** pour l'exécution réelle.
  - **Données (pilier « matière première ») :** `/v1/data/{profile,clean,clean-text,prepare-rag,prepare-corpus,prepare-training}` — profilage (types/nuls/distincts), nettoyage tabulaire (trim, coercition de types, valeurs manquantes, **déduplication**, lignes vides) + nettoyage de corpus texte, puis préparation vers RAG / corpus du LLM natif / table d'entraînement ML. Pur Python offline, clean-room (inspiration OpenRefine/KNIME).
  - **Apprentissage ML & inférence (pilier « algorithmes/modèles ») :** `/v1/neural/ml/{train,predict,models}` (classifieur supervisé **appris depuis exemples**, embeddings→softmax NumPy ou scikit-learn, **persisté**) ; `/v1/neural/inference` (catalogue des moteurs d'inférence locaux **Ollama/llama.cpp/vLLM/LM Studio/Triton** + provider natif `lunziko`, consommés via le Provider Manager OpenAI-compat).
  - **Activité (A-13) :** `/v1/activity/{log,log-batch,timeline,search,summary}` + `DELETE` — **journal des actions utilisateur** (les apps publient ce que fait l'utilisateur) = 1ʳᵉ brique de la **Couche de Contexte Unifié** (`CONTEXT_LAYER_ARCHITECTURE.md`, A-13→A-18) ; timeline + recherche sémantique + résumé, champ libre **chiffré** (AES-256-GCM), injecté dans le contexte des agents. **À venir : appstate (A-16), profil/RBAC consommé de Platform (A-14), assembleur de contexte (A-15), connecteurs RAG, catalogue de schémas (A-17), feedback (A-18), Action Registry (function calling).**
  - **Voix (V-1→V-4 RÉELLE) :** **TTS = Piper** (`/v1/voice/tts` → `audio/wav`, voix `.onnx` ex. fr_FR-siwis-medium 63 Mo, extra `voice-tts`, `AE_TTS_VOICES_DIR`) **+ STT = Whisper via faster-whisper** (`/v1/voice/stt`, modèle `base` 145 Mo `AE_STT_MODEL`, extra `voice-stt`) **+ MT V-3 = traduction** (`/v1/voice/translate`, MADLAD-400 on-device OU repli LLM via Provider Manager, extra `voice-mt`, `AE_MT_BACKEND`) **+ pipeline V-3 `/v1/voice/speak` (interprète STT→MT→TTS)** **+ packs V-4 téléchargeables** (`/v1/voice/packs/{id}/{plan,install}` : téléchargement réel par composant — voix Piper propre + Whisper/MADLAD partagés multilingues ; registre des composants installés). **Round-trip TTS→STT validé + download voix fr prouvé en réel** ; `/tts/status`+`/stt/status`+`/translate/status`. **18 langues** (fr/en/es/de/it/pt/ru/el/he/ar/hi/bn/ur/id/zh/ja/sw/ln).
  - **Génération multimédia (Brains génératifs image/vidéo/audio/3D) :** `/v1/generate/{status,backends,models/{kind},image,video,audio,music,3d}` — dispatche vers un backend génératif branché (Graphics Engine / ComfyUI / Fal / Replicate / diffusers GPU) ; sans backend, réponse `deferred` explicite (backends candidats + modèles du catalogue **MiniMax H3/Wan/Nano Banana** + note GPU/licence) — **jamais de génération simulée**. Les Brains image/video/audio/music/3d passent « declared »→« active » dès qu'un backend est disponible.
  - **Raisonnement avancé (logiques des IA open source, clean-room) :** `/v1/reasoning/{strategies,strategies/{id},reason}` — catalogue + implémentations **provider-agnostiques** des techniques OSS : **Chain-of-Thought, Self-Consistency (vote majoritaire), Tree-of-Thoughts, Reflexion (auto-critique+révision), Plan-and-Solve, Least-to-Most, Step-Back, ReAct, Multi-Agent Debate** (concepts publics réimplémentés, aucun code copié). Sélection automatique de stratégie. **Améliore le raisonnement ET renforce l'autonomie** : tourne sur le LLM natif `lunziko`, Ollama local ou tout provider (validé CoT en local Ollama).
  - **Mémoire persistante à apprentissage continu (« apprend toujours, n'oublie jamais ») :** `/v1/learning/{remember,observe,recall,reinforce,consolidate,timeline,archive,stats}` — mémoire long-terme **append-only** chiffrée (AES-GCM) au-dessus des ports : réapprendre un fait proche le **renforce** (répétition espacée) au lieu de le dupliquer ; **consolidation non destructive** (quasi-doublons liés à un canonique, jamais effacés) ; **rappel pondéré** (similarité × renforcement × importance) ; `archive` = oubli **soft** (tombstone rappelable via `include_archived`), **aucune suppression dure**. Complète la mémoire courte (`/v1/memory`), l'activité (`/v1/activity`) et le knowledge graph.
  - **Autonomie & résilience (personnalité « ne renonce jamais ») :** `/v1/autonomy/{solve,persona}` — boucle agentique **ReAct** (Réflexion→Action→Observation) qui **consulte la mémoire long-terme AVANT d'agir** (ne répète pas une erreur passée), **journalise échecs ET succès** dans la LTM (auto-évolution), **adapte** sa stratégie après échec et **n'abandonne jamais** (épuisement → PROBLÈME OUVERT mémorisé à retenter, jamais de « je ne sais pas, demande à l'utilisateur »). System prompt de détermination ; s'appuie sur Tools/Search/Code-Exec existants.
  - **Model Registry (catalogue des modèles d'IA + gouvernance licences) :** `/v1/model-catalog/{,stats,usable,restricted,code-tools,by-brain/{brain},{id}}` — catalogue des modèles concrets (dont la vague 2026 : **Qwen 3.8-Max** utilisable via provider OpenAI-compat ; **MiniMax H3**, **Wan 3.0**, **Wan2.2-Animate-14B**, **Nano Banana**, **CrisperWhisper 2.0**, **Gemini Robotics 2**, **Prisme.ai** en référence/restreint) **mappés aux Brains LAIA**, avec licence, poids ouverts, compat API, keyless et **restrictions juridiques** (ex. MiniMax H3 exclut UE/US/UK/Corée du Sud). Discipline clean-room : backends/références, aucun code copié. Ajoute le Brain `robotics`.
  - **Lunziko Code Intelligence (Code Brain élargi, « maîtrise tous les langages ») :** `/v1/code-intelligence/{languages,detect,index,search,understand,dependencies,symbols,projects,project/{p}}` — **catalogue 60+ langages** (généralistes/systèmes/web/mobile/data/fonctionnels/shells/requêtes/HDL/shaders/legacy) + détection par extension ; **indexation + recherche sémantique de code** ; analyse d'**architecture** (langages, points d'entrée, manifestes) ; extraction de **dépendances** (npm/pip/cargo/go/maven/spm/pub…) ; **symboles** multi-langages ; **contexte des 11 projets écosystème** (croisé avec CE registre). **Utilisable depuis tous les outils** via REST + **outils MCP `code_*`** + endpoint OpenAI-compat + **CLI `scripts/lzcode.py`** — recettes PowerShell/VS Code/Xcode/Visual Studio/Sublime/JetBrains/Android Studio/Docker/MS SQL/PostgreSQL/GitHub/Antigravity (`CODE_INTELLIGENCE_INTEGRATIONS.md`).
  - **Persistance :** ports SQLite/vector/blob ; **adaptateur Postgres/pgvector** (couplage optionnel Platform).
  - **SDKs multiplateformes (A-9) :** clients du gateway en **TypeScript** (`sdk/typescript/`), **Swift** (`sdk/swift/`, async/await macOS/iOS), **Kotlin** (`sdk/kotlin/`, JVM/Android), **Dart** (`sdk/dart/`, Flutter) — surface commune (chat/embed/rag/mémoire/agent/act-outils/écosystème/activité/contexte/assistant/handoff/neural/data/automation + get/post génériques). SDK Dart validé (`dart analyze` OK).
  - **À venir :** V-5 fine-tune voix lingála/swahili + voix enfants ; activation effective des Brains génératifs multimédias (modèles image/vidéo/3D dédiés + Graphics Engine) ; entraînement LLM natif à grande échelle (GPU) ; écriture/modification contrôlée de fichiers par Code Intelligence (avec garde-fous avant toute action destructive).
- **Expose :** raisonnement, RAG, agents, workflows, mémoire, code, voix, endpoints OpenAI/MCP. **Consomme :** **manifestes & fonctions de toutes les apps (ce registre)** pour accompagner/résoudre les tâches utilisateur.

### Lunziko Graphics Engine (LGE)

- **Catégorie :** moteur graphique / rendu (JSON-RPC, architecture à **32 agents** — Amélioration 2 : SceneOptimizer + MaterialCatalog + ClashDetection + RuleChecker + NURBS + GPUCompute + NVIDIA RTX + Physics + AI-Render + **O3DE Bridge** livrés).
- **Mission d'intégration (transverse) :** le LGE doit pouvoir **s'intégrer à TOUTES les applications Lunziko** (One, BI, DociaPub, VidiaPub, CAD, Yekoli, Platform, HUB, AI Engine, Design System) pour **les faire fonctionner correctement** (fournir rendus/graphiques/géométrie/PDF/CAD-BIM là où elles en ont besoin), **les améliorer** et **leur apporter de nouvelles fonctionnalités** graphiques. Intégration par **contrats d'API versionnés** (JSON-RPC/REST), jamais de couplage de code ; chaque app consomme les capacités du LGE via ce registre.
- **Intégration AI Engine (consommateur) :** le LGE doit pouvoir **appeler, faire fonctionner et utiliser les fonctionnalités de [[Lunziko AI Engine]]** — voire **embarquer l'AI Engine** — afin d'apporter les capacités IA à une application qu'il sert. Cas d'usage : agent **AI-Render** (27) qui délègue génération de vues / rendu photoréaliste / auto-fix de sketches à l'AI Engine ; assistance IA sur les rendus/scènes ; NL→opérations graphiques. **Accès via les endpoints AI Engine** (autonomes, OpenAI-compat `/v1/chat/completions`+`/v1/embeddings`, `/v1/agent/run`, `/v1/rag/*`, provider `lunziko` local) ou en **embarquant** le paquet `lunziko-ai-engine`. Découplé (repli si AI Engine indisponible), clean-room, licences/identité restant à Platform.
- **Statut :** **1067 tests ✅** (32 agents ; Amélioration 2 COMPLÈTE : agents 23→30 + RTX natif + passerelle O3DE). Codes d'erreur JSON-RPC **-32000 → -32022** + **-32023** (nurbs) + **-32024** (gpu) + **-32025** (clash) + **-32026** (rule) + **-32027** (airender) + **-32028** (physics) + **-32029** (material) + **-32030** (scene) + **-32031** (rtx) + **-32032** (o3de). Seul dossier à modifier pour le LGE.
- **Fonctions exhaustives (agents & capacités) :**
  - **Imaging** (Pillow+numpy, `IMAGING_ERROR=-32012`).
  - **Asset** (trimesh, **scènes multi-objets**, `ASSET_ERROR=-32013`).
  - **Vector** (pur Python) — 10ᵉ agent.
  - **PDF** (génération, pur Python) — 11ᵉ agent.
  - **CAD** (C1, OCCT).
  - **BIM** (C3, IfcOpenShell).
  - **Sketch** paramétrique (CAO : contraintes 2D Levenberg-Marquardt numpy + **extrude/revolve** OCCT, `SKETCH_ERROR=-32022`) — 22ᵉ agent.
  - **Workflow** (é37/é38).
  - **Phase E — DAO/VR :** Integrity, Accel, VR.
  - **Phase F — moteur de jeu :** Cache, Shader, Catalog.
  - **Registre moteurs :** vision ~40 moteurs (`docs/SPECIFICATION_LGE.md`, `MOTEURS_REFERENCE.md`, `manager/engines.py`).
  - **Agents Amélioration 2 LIVRÉS (pur Python, sans install) :**
    - **SceneOptimizer** ✅ (`SCENE_ERROR=-32030`, numpy/trimesh) — `scene.stats` (stats + recommandations), `scene.instancing` (géométries dupliquées→instances), `scene.bvh` (BVH AABB median-split 2D/3D), `scene.lod` (LOD multi-niveaux). 9 tests.
    - **MaterialCatalog** ✅ (`MATERIAL_ERROR=-32029`, pur Python) — `material.list`/`material.get` (bibliothèque 10 matériaux PBR + propriétés physiques BIM densité/conductivité/norme), `material.pbr` (validation/normalisation PBR + complétude), `material.export` (JSON). 7 tests.
    - **ClashDetection** ✅ (`CLASH_ERROR=-32025`, numpy) — `clash.detect` (intersections AABB entre objets, dur/clearance, volume de recouvrement, filtre par discipline, rapport), `clash.pair` (2 boîtes : recouvrement/volume/séparation). 5 tests.
    - **RuleChecker** ✅ (`RULECHECK_ERROR=-32026`, pur Python) — `rule.rulesets` (jeux intégrés accessibilité/structure/MEP), `rule.check` (vérifie éléments : pass/fail/na + règles personnalisées). 6 tests.
    - **NURBS** ✅ (`NURBS_ERROR=-32023`, OCCT/OCP LGPL, process séparé) — `nurbs.curve` (courbe B-spline/NURBS clampée depuis points de contrôle, poids rationnels, échantillonnage + longueur, export STEP/IGES), `nurbs.surface` (surface B-spline depuis grille, bbox, export STEP/IGES), `nurbs.tessellate` (surface → STL). Extension géométrique du CAD Kernel. 8 tests.
    - **GPUCompute** ✅ (`GPU_ERROR=-32024`) — calcul GPU réel via **wgpu (WGSL : Vulkan/Metal/DX12/WebGPU)** + **repli CPU garanti** (numpy). `gpu.status` (adaptateur GPU + CUDA), `gpu.compute` (ops élémentaires scale/square/sqrt/add/sub/mul/saxpy, backend auto/gpu/wgpu/cuda/rtx/cpu). **Validé en réel** (saxpy sur Intel Iris Xe via Vulkan). **2ᵉ backend NVIDIA RTX/CUDA** via CuPy (`backend=cuda/rtx`, actif sur machine NVIDIA). 9 tests. extra `gpu` (`wgpu>=0.32`).
    - **NVIDIA RTX (moteur natif COMPLET, 2ᵉ moteur GPU)** ✅ (`RTX_ERROR=-32031`) — **stack open-source NVIDIA RTX intégré nativement** (pas un wrapper) : sous-projet `native/rtx/` avec **submodules NVRHI+Donut+RTXGI+RTXDI+NRD+RTXMU+RTX Remix+PhysX+Flow**, superbuild CMake, service natif C++ `rtx_service` (JSON-RPC), scripts de build (`setup_submodules.ps1`/`build.ps1`). Agent Python `rtx` proxy : `rtx.status` (stack + binaire détecté), `rtx.render` (path tracing), `rtx.denoise` (NRD), `rtx.simulate` (PhysX/Flow). **Compile sur machine NVIDIA** (agent rapporte « non compilé » sinon, -32031). Clean-room : dépôts en submodules, aucune source tierce copiée. 3 tests. Docs `native/rtx/README.md`+`PROTOCOL.md`.
    - **Physics** ✅ (`PHYSICS_ERROR=-32028`, pur numpy CPU) — `physics.simulate` (corps rigides sphériques : gravité, intégration semi-implicite, collisions sol + sphère-sphère par impulsions, trajectoire optionnelle), `physics.raycast` (rayon vs sphères, plus proche impact), `physics.collide` (collision élastique 2 corps). Chemin GPU (PhysX/Flow) = via `rtx.simulate`. 6 tests.
    - **AI-Render** ✅ (`AIRENDER_ERROR=-32027`) — IA graphique : `airender.nl` (langage naturel → opérations graphiques extrude/revolve/rotate/fillet/chamfer/render, **parseur local hors-ligne**), `airender.autofix` (heuristique DOF de contraintes manquantes d'un sketch, **local**), `airender.render` (génération d'image **déléguée au [[Lunziko AI Engine]]** `/v1/generate/image`, erreur claire si injoignable), `airender.status` (joignabilité AI Engine, `LUNZIKO_AI_ENGINE_URL`). urllib stdlib, aucune dépendance ajoutée. 7 tests.
    - **O3DE Bridge** ✅ (`O3DE_ERROR=-32032`) — **banc d'essai réel** de rendu (Atom) et physique (PhysX) via **O3DE (Open 3D Engine, Apache-2.0)** : agent client socket `o3de.status`/`o3de.spawn` (entité PhysX)/`o3de.simulate` (game mode, N frames, transforms)/`o3de.render` (capture Atom) + **script in-editor `native/o3de/lunziko_o3de_bridge.py`** (serveur socket via Editor Python Bindings `azlmbr`, à lancer dans O3DE). Sans passerelle en écoute → « non connecté » (erreur claire). socket/json stdlib. 2 tests. Docs `native/o3de/README.md`+`PROTOCOL.md`.
    - Tous câblés agent/manager/engines/CLI (`sceneopt`/`material`/`clash`/`rule`/`nurbs`/`gpu`/`rtx`/`physics`/`airender`/`o3de`)/API (`/scene/*`,`/material/*`,`/clash/*`,`/rule/*`,`/nurbs/*`,`/gpu/*`,`/rtx/*`,`/physics/*`,`/airender/*`,`/o3de/*`).
  - **Amélioration 2 — série d'agents V2 (23→30) + RTX natif : COMPLÈTE** (9 agents livrés, tous testés). Reste = modules « moteur universel » (backends GPU DX12/Metal, vidéo/audio temps réel, chroma, texte pro, multi-sorties…) hors série d'agents.
  - **Roadmap :** Phase D = GPUCompute/Physics/AI-Render (rendu/physics pybullet/GPU/AI onnxruntime) · Phase E+ = ClashDetection/RuleChecker (+ extraction géométrie ifcopenshell.geom) · Phase F+ = MaterialCatalog/SceneOptimizer · NURBS = extension Geometry/CAD (OCCT).
  - **AMÉLIORATION 2 — « moteur universel » (SPEC, `docs/SPECIFICATION_LGE_AMELIORATION_2.md`, NON codé) :** évolution du LGE en **moteur hybride Temps réel + CAO/BIM + Génératif IA + Vidéo + Audio + Simulation + Multi-sorties**, pour servir **VidiaPub** (suite créative), **Live Stream** (production live) et **Lunziko CAD v2** (Archi + CAO + BIM + Simulation + Horlogerie). 3 CDC consolidés → **42 capacités manquantes en 14 modules critiques** :
    1. **Backend GPU unifié** (DirectX12 + Metal + Vulkan + WebGPU) — couche d'abstraction, render graph moderne (type UE5), shaders cross-platform HLSL/MSL/GLSL→SPIR-V, multi-render-targets.
    2. **Moteur 2D temps réel** (calques GPU, blend modes, masques vecteur+pixel, transitions GPU, timeline 2D, templates lower-thirds/tickers/overlays).
    3. **Moteur vidéo temps réel** (decode/encode GPU H.264/HEVC/AV1, frame graph, scopes waveform/vectorscope, proxies, multicam).
    4. **Moteur audio DSP** (EQ/compresseur/limiteur/de-esser/ducking, noise-reduction IA, sync A/V, waveform GPU, automation).
    5. **Chroma key / segmentation IA / masques dynamiques** (chroma GPU, segmentation SAM-like, background removal IA).
    6. **Texte professionnel** (layout OpenType, SDF text GPU, styles, sous-titres animés).
    7. **Vectoriel avancé** (Bézier complet, boolean ops GPU, SVG/EPS import/export, hachures intelligentes, snapping temps réel, vectorisation IA).
    8. **CAO avancée** (NURBS complet, kernel géométrique interne optimisé OCCT++/WASM, tessellation GPU, découpes/section planes, exploded views).
    9. **BIM** (IFC/BCF, ClashDetection, RuleChecker, extraction géométrie IFC GPU, matériaux/snapping/sectioning BIM).
    10. **Simulation** (FEA/thermique/fluides GPU, optimisation topologique IA).
    11. **Rendu photoréaliste** (path tracing GPU, denoiser IA OptiX/OIDN, pipeline HDR, sun/sky, MaterialCatalog, HDRI).
    12. **IA graphique** (AI-Render : NL→ops graphiques, auto-rendu/inpainting/auto-framing, croquis→CAO, photo→vecteur, plan 2D→BIM, transitions IA).
    13. **SceneOptimizer** (LOD auto, instancing, BVH 2D/3D, streaming d'assets, cache GPU partagé, multi-résolution).
    14. **Multi-sorties** (16:9 + 9:16 simultané, multi-render-targets, pipelines parallèles, sync multi-flux).
  - **Sources externes à intégrer (clean-room / dynamique selon licence, aucune copie de source copyleft) :** **DiligentEngine** (Apache-2.0 — abstraction GPU DX12/Vulkan/Metal/WebGPU, HLSL universel→GLSL/MSL/SPIR-V, ray tracing/async compute ; intégrable) · **NVIDIA NVRHI** (MIT — RHI DX12/Vulkan + ray tracing ; intégrable) · **NVIDIA-RTX** (RTX Remix / PhysX / Flow / Streamline DLSS-Reflex / Open GPU Kernel Modules — licences mixtes MIT/BSD + NVIDIA Software License ; binaires DLSS gratuits **redistribuables sans royalties**, ray tracing via DXR/VK_KHR_ray_tracing standard) · **openh264** (Cisco, BSD-2 ; binaire H.264, royalties couvertes par Cisco) · **libde265** (⚠️ **LGPL/GPL + brevets HEVC** → **process séparé / étude seulement**, jamais de lien statique) · **anime4k** (MIT — upscaling temps réel par shaders) · **4KAgent** (super-résolution IA 4K, licence à vérifier avant install) · **litecode** (à évaluer). SDKs GPU **Vulkan/Metal installés sur le poste** ; **wgpu-native** (releases) pour WebGPU ; **DirectX12** via Visual Studio. **O3DE (Open 3D Engine, Apache-2.0)** disponible/ouvert sur le poste de Joe → **banc d'essai de rendu temps réel + entraînement graphique** et **référence permissive** pour la couche GPU (modules 1/11) et Physics (module 10, PhysX intégré) ; peut servir de backend de rendu/validation pour AI-Render/GPUCompute quand ces modules seront implémentés.
  - **Codes d'erreur :** réserver un nouveau bloc à partir de **-32031** pour les modules Amélioration 2 (au-delà de -32030 des agents V2).
- **Expose :** rendus haute qualité (graphiques/visuels), imaging, vector, PDF, CAD/BIM/sketch. **Exposera (roadmap V2) :** NURBS, GPUCompute, ClashDetection, RuleChecker, AI-Render, Physics, MaterialCatalog, SceneOptimizer. **Exposera (Amélioration 2 « moteur universel ») :** backends GPU (DX12/Metal/Vulkan/WebGPU), 2D/vidéo/audio temps réel, chroma/segmentation IA, texte pro, vectoriel avancé, path tracing PBR/HDR, simulation multiphysique (FEA/thermique/fluides), IA graphique (NL→ops, croquis→CAO, plan→BIM), SceneOptimizer + cache GPU partagé, multi-sorties 16:9+9:16, formats CAO/BIM (STEP/IGES/IFC/BCF/DWG*) + vectoriel (SVG/EPS/PDF) + raster pro (HDR/ICC/LUT). **Consomme :** spécifications de rendu émises par les apps (ce registre pour connaître leurs besoins) ; **fonctionnalités de l'AI Engine** (AI-Render, génération textures PBR, segmentation/vectorisation IA, NL→ops, denoising, auto-fix sketch, croquis→CAO, plan 2D→BIM — via endpoints ou paquet embarqué) ; **SDKs GPU** (Vulkan/Metal/DX12/wgpu-native), denoiser OptiX/OIDN, codecs GPU (VideoToolbox/Media Foundation/NVDEC-NVENC), OCCT + IfcOpenShell (déjà présents), sources externes clean-room (DiligentEngine/NVRHI/NVIDIA-RTX/openh264/anime4k…). **Sert directement :** VidiaPub, Live Stream, Lunziko CAD v2 (+ toutes les apps via la mission d'intégration transverse).

### Lunziko Design System (LDS)

- **Catégorie :** plateforme de conception unifiée de **toute** la suite Lunziko.
- **Statut :** monorepo pnpm+Turborepo+Changesets+TS strict (`github.com/jaisebukadilu/lunziko-design-system`, privé). **Phases 0→7 faites** ; build 9/9 · typecheck 13/13 · test 13/13.
- **Fonctions exhaustives :**
  - **Tokens :** `@lunziko/tokens` (DTCG 3 étages → Style Dictionary → **CSS / TS / Swift / Kotlin / Dart / XAML WinUI+WPF / GTK CSS / gpu JSON**) × thèmes **dark / light / high-contrast**.
  - **Composants (19+ React + parité Web Components + Flutter partiel) :** Button, TextInput, Checkbox, Radio, Switch, Select, Dialog, Badge, Alert, Spinner, Tooltip, BrandHero, Divider, Card, Progress, Breadcrumb, Logo, BrandFooter (+ batch2 à venir : Tabs, Menu, Table).
  - **Densité :** comfortable / compact (`[data-density]`).
  - **Marques :** **7 chartes de marque exclusives** (`[data-brand][data-theme]`) : vidiapub, cad, hub, one, yekoli, dociapub, **sentinel** (re-résolution des tokens composant).
  - **Logos :** règle des logos (logo société Lunziko obligatoire + services connectés même ligne ; le LDS ne bundle pas les logos d'autrui) — manifeste + BrandFooter.
  - **Doc & CI :** site `@lunziko/docs` (Vite+React : Design Language/Foundations/Tokens explorer/Composants, bascules thème+densité) ; CI GitHub Actions ; Storybook, Vitest+axe, Playwright.
- **Expose :** tokens & composants (build-time, aucun appel runtime). **Consomme :** chartes/marques & besoins UI des apps (ce registre).

### Lunziko One

- **Catégorie :** suite **ERP** métier modulaire IA-native (FR + CH). Adossée à Platform.
- **Statut :** CDG v4.0, charte « Ether » (glassmorphism dark, gradient **#2E7CFF → #9D50FF**, Inter), ~42 maquettes Stitch (définitives), interface web `apps/one-web/` (Next.js 14) **démarrée** (Connexion, Dashboard global, Finance compta/overview + Facturation). Persistance multi-SGBD (PostgreSQL/MySQL/MariaDB via ORM). Clean-room inspiré MS D365 BC.
- **Fonctions exhaustives (12 modules, V1→V5) :**
  - **Finance :** CA, revenus, dépenses, charges, bénéfices/pertes, trésorerie, comptes, écritures, budgets, prévisions, paiements, factures (FR : PDP/PPF ; CH : QR-facture), fournisseurs, clients, dimensions analytiques multi-axes, centres de coûts, gestion devises & réévaluation, rapprochement bancaire.
  - **Comptabilité :** journaux, comptes, mouvements, périodes, balances, résultats, bilan, compte de résultat, clôtures.
  - **Ventes (Sales) :** devis→commande→livraison→facture, produits, clients, commerciaux, régions, marges, remises & tarifs échelonnés, retours/avoirs.
  - **Achats (Purchase) :** commandes fournisseurs, fournisseurs, achats, prix, délais, coûts, workflows d'approbation.
  - **Stocks (Inventory) :** stocks, mouvements, entrées/sorties, inventaires, ruptures, rotations, valorisation (FIFO/moyenne pondérée), emplacements/entrepôts, réappro & points de commande, traçabilité lot/série.
  - **RH :** effectifs, départements, postes, contrats, ancienneté, absences, congés, masse salariale, recrutement, turnover (données sensibles soumises à la sécurité Platform).
  - **Payroll (Paie).**
  - **CRM** (relation client), **Projects** (jobs/tâches, WIP, rentabilité), **Manufacturing/Service** (roadmap : BOM, ordres de production, contrats de service).
  - **Analytics :** entrée **« Analytics » → ouvre Lunziko BI** ; **module client léger embarquant les dashboards BI** ; **deep-linking** (ex. facture → analyser dans BI).
  - **Marketplace (V5).**
  - **Transverse :** e-documents (facturation électronique), subscription billing, workflows d'approbation configurables.
- **Expose :** données métier + événements (`finance.entry.posted`, `payroll.run.closed`, `sales.order.confirmed`…) via **Lunziko Data API**. **Consomme :** BI, DociaPub, AI Engine, Graphics Engine, Platform (auth/licences/signature), HUB.

### Lunziko BI

- **Catégorie :** **Business Intelligence / Data Analytics / Reporting** — couche Data/Analytics commune de l'écosystème.
- **Statut :** CDC v1.1, charte « Lunziko BI Design System » (dark Command Center, Hanken/Inter/JetBrains, primary **#0062FF**), 28 maquettes Stitch (21 desktop + 7 mobile). Design+spec. Fichiers `INTEROPERABILITE_ECOSYSTEME.md` + `MEMOIRE_PROJET.md` présents.
- **Fonctions exhaustives :**
  - **Données & modélisation :** connexion de sources (fichiers, **SQL**), import **XLSX/CSV/ODS/JSON**/tabulaires Lunziko, **datasets**, **data models**, **mesures**, catalogue de données certifiées.
  - **Restitution :** **rapports** (éditeur de rapports), **dashboards** (dashboard principal / exécutif), **KPIs**, data explorer, **graphiques dynamiques embarquables** (liés à la source).
  - **Connecteurs natifs :** **Lunziko One Connector** (découverte auto selon droits), **MySheet Connector**, **MyData Connector**, connecteur écosystème.
  - **Data Layer / Data API :** `/api/v1/{data,datasets,sources,tables,schema,analytics}` ; datasets partagés (Single Source of Truth) ; synchronisation temps réel / quasi temps réel / planifiée / manuelle.
  - **Dashboards prêts à l'emploi :** Direction, Finance, Commercial, RH, Stock.
  - **IA :** **AI insights** / smart insights, **assistant vocal IA**, NL→requête, résumés exécutifs, prévisions, **orchestrateur IA écosystème**, copilot cross-app, automatisation des rapports financiers.
  - **Simulation :** **what-if scenarios**, simulation de scénarios stratégiques.
  - **Gouvernance & sécurité :** tableau de bord de gouvernance, souveraineté des données, **lignage & traçabilité** des données, sécurité **Lunziko Data Layer**, **héritage des droits de la source** (BI ≤ droits source).
  - **Intégrations DociaPub :** intégration MySheet, éditeur MyWord intégré, synthèse stratégique MyWord, générateur MySlides, publication MyPublish, partage via MyMail.
  - **Mobile :** dashboard exécutif, alertes intelligentes, collaboration temps réel, exploration **AR**, lignage des données, **monitoring IoT/edge**, assistant vocal.
  - **Export/partage :** PDF, CSV, MySheet, MyWord, MySlides, MyMail, MyPublish.
  - **Licences :** paliers **Free** (local) / **Professional** (One) / **Business** (One+DociaPub) / **Enterprise** (API, embedding, data governance).
- **Expose :** analytics, dashboards, graphiques liés, API BI, embedding. **Consomme :** One, DociaPub, Data Layer, AI Engine, Graphics Engine, Platform, HUB.

### DociaPub (Lunziko DociaPub)

- **Catégorie :** suite **documentaire / bureautique** (cible macOS + Windows, moteur OOXML/ODF ; legacy MyOfficeSuite Swift 43k LOC comme référence).
- **Statut :** phases 1 & 2 livrées (cartographie + **moteur documentaire DDM** hybride, filtres OOXML/ODF, layout→RenderPort, **8 ports** définis) ; socle monorepo TS+Tauri (app pilote MyWord). Design, rien codé.
- **Fonctions exhaustives (7 applications de la suite) :**
  - **MyWord** — traitement de texte (rédaction, styles, tableaux, graphiques liés BI, export PDF/DDM, génération de rapports analytiques).
  - **MySheet** — tableur (feuilles, plages, tri/filtre/transformation, formats XLSX/CSV/ODS/JSON, **« Analyser avec Lunziko BI »**, export/import BI).
  - **MySlides** — présentations (slides, graphiques BI liés, génération auto de présentations IA).
  - **MyMail** — email (envoi de rapports : lien sécurisé / PDF / image / résumé IA).
  - **MyNotes** — notes (analyses, observations, hypothèses, conclusions ; IA → notes structurées).
  - **MyData** — base de données structurée (source de datasets pour BI).
  - **MyPublish** — PAO / publication professionnelle (rapports annuels/financiers/d'activité, brochures, documents institutionnels).
  - **Transverse :** rendu web/canvas façon ONLYOFFICE, moteur doc façon LibreOffice/AOO, coffre-fort, signature via service Platform.
- **Expose :** génération/édition/rendu de documents ; MySheet/MyData = sources ; MyWord/MySlides/MyMail/MyPublish = cibles. **Consomme :** BI, AI Engine, Graphics Engine, Platform (auth/signature), HUB.

### Lunziko VidiaPub

- **Catégorie :** suite **créative** (macOS + Windows ; nom interne « Lunziko »).
- **Statut :** macOS v2.0 abouti (**148 fichiers Swift**) ; Windows WinUI 3 = squelette (~5 %). Charte « VidiaPub Core » (Dark Studio, Inter/JetBrains, Blue-600 #007AFF). Socle préparé pour greffes AI/Design/Graphics/Platform. **Docs de cadrage 2026-08-08/09 : `CAHIER-DES-CHARGES.md`, `ANALYSE-CHARTE-STITCH.md`, `ARCHITECTURE-CIBLE-VIDIAPUB.md` (racine projet).**
- **Fonctions exhaustives (6 modules) :**
  - **Publisher** — PAO / mise en page **créative** (magazines, brochures marketing, affiches, catalogues) ; distinct de DociaPub/MyPublish (institutionnel).
  - **Photo** — retouche & édition d'images (calques/masques/courbes, correction optique, RAW, HDR ; sous-module Photo AI : débruitage/sky/objet/inpainting/upscale).
  - **PDF** — « PDF Studio » : création/édition/formulaires/manipulation + PDF/A-X, signature, OCR.
  - **Logo** — création de logos, **scope élargi Design + Vector + Brand Kit** (orientation Design/violet ; décision projet D-01, renommage officiel à confirmer ici avant d'appliquer).
  - **Vidéo** — montage multipiste, étalonnage/scopes, motion, IA (STT/sous-titres/reframing) ; encode VideoToolbox HEVC/ProRes.
  - **Audio** — DAW/éditeur (6ᵉ module, D-02 : multipiste/EQ/mixage/mastering ; piste réutilisable dans Vidéo).
  - **Transverse :** shell **Metal**, **SpaceMouseDriver**, contrats « partagés, impl. natives » (Swift/C#), tokens design générés. **Socle cible : `VidiaGraphicsCore` (moteur rendu local partagé) + formats natifs `.vidia`/`.v*` + « VidiaPub AI » = couche CLIENTE de l'AI Engine (assistant scopé `/v1/assistant/vidiapub/ws`, Action Registry, App Requirements image/video/vision/audio).**
- **Décisions projet (2026-08-09) :** D-01 Logo→orientation Design/Vector/Identity · D-02 Audio = 6ᵉ module · D-03 **Lunziko HUB** au périmètre distribution · D-04 « MyOfficeSuite » → **DociaPub** (appliqué). 1ᵉʳ code clean-room ajouté : `Modules/Photo/PhotoLensCorrection.swift` (correction optique Core Image).
- **Expose :** création graphique/vidéo/audio/PDF/logo (+ **manifeste** aux 5 agrégateurs, règle §0.2, à brancher). **Consomme :** AI Engine (assistant+LAIA), Design System (`@lunziko/tokens`, marque `vidiapub`), Graphics Engine (imaging/vector/pdf/asset), Platform (auth/**licences**/signature), HUB (distribution/gating).

### Lunziko CAD

- **Catégorie :** **CAO / DAO** (Architecture + CAO mécanique).
- **Statut :** monorepo pnpm/Turborepo + TS + Tauri/Rust + React + three.js + opencascade.js (8 phases). Phase 0 (socle web) faite & validée ; charte « Technical Precision » (fond #071424, accent **#1e6fe0**, AI cyan **#00F5FF**, argent #C0C0C0). Phase 1 en pause (statut LGE à trancher).
- **Fonctions exhaustives :** modélisation 2D/3D **paramétrique**, **contraintes**, **extrude/revolve**, rendu **three.js**, **app shell 3 panneaux**, architecture + mécanique ; consomme potentiellement le **Graphics Engine** (moteur sketch/CAD/BIM) ou un LGE Python séparé (décision en attente).
- **Expose :** modèles CAO/DAO. **Consomme :** Graphics Engine, AI Engine, Design System, Platform, HUB.

### Lunziko Yekoli

- **Catégorie :** plateforme d'**apprentissage des langues**.
- **Statut :** Next.js 14 + Supabase + Vercel. **Phase 0 livrée** (squelette, auth, migrations RLS West EU, thème Yekoli, Freemium, domaine `yekoli.lunziko.app`). GitHub `jaisebukadilu/Lunziko-Yekoli`.
- **Fonctions exhaustives :** apprentissage des langues, **Language Engine hexagonal (FSRS)** (répétition espacée), leçons/progression, mobile **RN/Expo**, corpus (**lingála**), modèle **Freemium**. MVP Web EN+FR à venir (Phase 1).
- **Expose :** contenus & progression d'apprentissage. **Consomme :** AI Engine, Design System, Platform, HUB.

### Lunziko Sentinel

- **Catégorie :** **cybersécurité souveraine** — antivirus / EPP / EDR / XDR multi-plateforme (code 100 % propriétaire, aucune licence tierce). Console de sécurité web = **Sentinel Platform**.
- **Statut :** **Design + spécification** (CDC v1.0 = `Lunziko Sentinel\CAHIER_DES_CHARGES_LUNZIKO_SENTINEL.md`). Charte « **Sentinel Industrial Cyber** » (Cyber-Industrial dark, Obsidian #050505 / Industrial Gray #1A1A1A, **Cyber Blue #00A3FF**, Electric Red #FF0000, Forest Green #2ECC71, Metallic Silver #C0C0C0 ; Hanken/Inter/JetBrains ; ⚠️ conflit primary #adc6ff M3 vs #00A3FF marque → **ADR-SENT-001** = #00A3FF recommandé). ~90 maquettes Stitch (onboarding/desktop privé/Platform web/mobile/simulations) + logo produit (bouclier casque spartiate argent→bleu) + 5 icônes systray. **RIEN codé.** Clean-room (inspiration ClamAV/YARA/Wazuh/OSSEC/OpenSnitch + suites Bitdefender/Norton/ESET/CrowdStrike/SentinelOne, aucun code copié).
- **Architecture duale :** **Privé** = interface de bureau complète (toutes tâches locales) + page de gestion de parc perso sur Sentinel Platform (actions distantes, multi-licences famille) · **Entreprise** = agent **headless** (silencieux, uniquement icône systray), administration **100 % web** via Sentinel Platform, déploiement de masse (scripts PowerShell/Bash/Python + jetons). Vaut pour desktop ET mobile.
- **Plateformes :** Desktop (Windows/macOS/Linux), Serveur (Windows/Linux), Mobile (Android/iOS), Console web (Sentinel Platform SaaS multi-tenant).
- **Fonctions exhaustives :**
  - **Agent local / UI privée :** NGAV temps réel (« Antivirus Neuronal ») — virus/vers/troyens/rootkits/spyware/ransomware ; Advanced Threat Defense (comportemental, zero-day, fileless) ; **bouclier anti-ransomware + rollback** ; anti-exploit ; protection hors-ligne. Scans **Rapide / Approfondie / Personnalisée** + planifiés + USB. **Contrôle des périphériques** (USB/Bluetooth). **Pare-feu applicatif bidirectionnel** (« Pare-feu Quantique »). **Vault** : protection webcam/micro, gestionnaire de mots de passe, coffre chiffré, anti-pistage, prévention menaces en ligne (phishing), optimiseur système. **Journaux/quarantaine** (flux terminal temps réel). Navigation privée : Vue d'ensemble · Analyse des Menaces · Incidents · Règles de Sécurité · Journaux & Quarantaines · Portail Web.
  - **Sentinel Platform (web) :** tableau de bord global (score de santé du parc) · **gestion de flotte / santé endpoints** · supervision multi-appareils · **EDR** (télémétrie comportementale, arbre/chaîne d'attaque, graphes de propagation) · **Mapping MITRE ATT&CK** · **gestion des incidents** (cycle de vie) · **Cloud Sandbox forensique** (détonation isolée + scoring IA) · **actions à distance** (isoler/nettoyer/arrêter processus/supprimer/redémarrer) · **Remote Rescue Mode** (nettoyage rootkits/bootkits avant OS) · **configuration réponse automatique IA** (playbooks SOAR) · **anti-vol** (géolocalisation/verrouillage/alerte sonore/effacement/restauration) · **gestion des règles** (Firewall/HIDS/politiques) · onboarding/activation licence · **centre de déploiement Enterprise** (jetons + scripts d'enrôlement) · **rapports de conformité/audits** + rapports MITRE · consoles privée & entreprise (RBAC multi-tenant).
  - **Sentinel Mobile :** tableau de bord + santé IA du parc · analyse des menaces + analyse IA détaillée · **analyse de risque applicatif** (permissions) · incidents + rapport d'incident IA · quarantaine & journaux · actions à distance · anti-vol · paramètres · mode entreprise silencieux.
  - **Fondations (clean-room) :** parseur des définitions **ClamAV** (main.cvd/daily.cvd publics) + moteur de règles compatible **YARA** ; HIDS/FIM inspiré **Wazuh/OSSEC** ; pare-feu inspiré **OpenSnitch** ; défense comportementale + IA Lunziko. **Threat Intelligence gratuite** : **MalwareBazaar** + **ThreatFox** (abuse.ch) + **AlienVault OTX** (clés API à stocker en secret via coffre Platform ; service *Threat Intel Ingestor* → définitions signées poussées via HUB). ⚠️ vérifier licences OSS avant tout import (privilégier formats/interfaces publics, pas de reprise de code).
  - **États systray :** Protégé · Analyse · Alerte/menace · Mise à jour · Désactivé/déconnecté.
- **Expose :** état de protection des endpoints, alertes/incidents, IOCs, rapports de conformité, actions distantes (Action Registry), événements `sentinel.threat.detected`/`sentinel.incident.opened`/`sentinel.device.isolated`… (bus Platform + webhooks). **Consomme :** **Platform** (identité, **licences/achat**, signature, bus — autorité unique), **HUB** (distribution/MàJ/gating), **AI Engine** (assistant scopé `sentinel`, scoring/analyse comportementale, raisonnement/playbooks, RAG KB menaces, safety/redaction PII, search OSINT, Action Registry), **Graphics Engine** (graphes d'attaque/PDF/cartes), **Design System** (tokens + marque `sentinel` **ajoutée** au LDS le 2026-08-22 : `packages/tokens/src/brands/sentinel/`, dark-first, primary Cyber Blue #00a3ff, scope `[data-brand="sentinel"]`), flux Threat Intelligence externes.

### Lunziko Live Stream

- **Catégorie :** **pôle Live Media** de l'écosystème — plateforme de **production, réalisation, enregistrement et diffusion vidéo en direct multiplateforme** (studio live + régie multicaméra + multistream + serveur auto-hébergeable). Cible : créateurs, entreprises, événements, églises, gaming, webinaires, podcasts.
- **Statut :** **Design + spécification** (CDC v1.1 = `Lunziko Live Stream\Lunziko_Live_Stream_Cahier_des_Charges.md`). Charte « **Lunziko Live Pro** » (Mission-Control dark industriel ; canvas #050608 / surface #121317 / panneau #14171D ; **primary electric-blue #00CCFF**, program-red #FF0032 [LIVE/REC], preview-green #2ECC71 [standby], metallic-silver #C0C0C0, warning-amber #F1C40F ; Inter + JetBrains Mono ; ⚠️ conflit variante « Broadcast Pro » primary M3 #adc7ff → **résolu : #00CCFF marque l'emporte**). ~45 maquettes Stitch (desktop production 16:9 & vertical 9:16, mobile caméra, serveur/admin, player web, guest, enterprise, IA director/clips, landing/tarification) + guide admin + **logo produit** (`_logo/`). **RIEN codé.** Clean-room (inspiration OBS Studio, datarhei Restreamer, Owncast, OSP, MediaMTX, obs-multi-rtmp, Aitum Multistream, SplitCam, Restream/StreamYard/Streamlabs ; FFmpeg/WebRTC ; **SBOM par version**, vérification licences avant tout import — aucun code copié).
- **Plateformes :** Desktop (**Windows 11 x64/ARM64**, **macOS** Apple Silicon/Intel), Mobile/Tablette (**iOS** iPhone/iPad, **Android**), Serveur (**Linux/Docker/Docker Compose** — VPS/dédié/NAS/cloud privé). Modes : **local**, **serveur distant**, **hybride**, **production mobile**.
- **Fonctions exhaustives :**
  - **Production / Studio :** sources vidéo multiples (webcam, USB, capture HDMI, écran/fenêtre, navigateur, fichier/image/GIF, caméra IP, RTSP/RTMP/SRT/WebRTC, smartphone) ; **Scene Engine** (scènes, calques, groupes, transitions, filtres, effets IA, import/export) ; réalisation **Preview/Program** (Cut/Fade/Stinger/Auto) ; **mixeur audio** (volume/mute/solo/gain/pano/VU/monitoring + noise gate/suppression/EQ/compresseur/limiteur/de-esser/ducking) ; **production double horizontale 16:9 + verticale 9:16** (scènes/overlays/cadrages/destinations propres).
  - **Lunziko Camera (mobile) :** smartphone → caméra réseau contrôlée (avant/arrière, autofocus, exposition, WB, zoom, flash, résolution/FPS/orientation) ; appairage **Wi-Fi/QR/code/LAN/Internet via serveur** ; télémétrie studio (connexion, qualité, FPS, débit, latence, batterie, température).
  - **Diffusion (Multistream Engine) :** multistream simultané YouTube/Twitch/Facebook/LinkedIn/Kick/TikTok/Instagram (selon API) + **RTMP/RTMPS/SRT personnalisés** + serveur Lunziko + site web ; profils d'encodage (Master/High/Balanced/Low-Bandwidth/Custom) ; **métadonnées par plateforme** ; stockage sécurisé des stream keys (jamais en clair).
  - **Graphics & habillage :** intégration **Lunziko Graphics Engine** (composition 2D, texte, SVG, formes, animations, transitions, masques/alpha, **chroma key**, shaders, color-correction, overlays, lower-thirds, ticker, logo, compteurs) ; templates réutilisables (lower-third/logo/watermark/écran attente-pause-fin/alertes/donation/sponsor) ; **GPU abstrait** (DirectX/Metal/Vulkan + encodage matériel).
  - **IA (Lunziko AI Engine) :** **AI Director** (détection locuteur/visages, switch caméra/scène auto, cadrage, transitions) ; **AI Camera** (tracking, auto-framing/crop, suppression/flou/remplacement de fond) ; **AI Audio** (débruitage, amélioration voix, anti-écho, normalisation) ; **AI Content** (transcription, chapitrage, résumé, **highlights + clips/Shorts/Reels**, sous-titrage, traduction) — fournisseur externe optionnel.
  - **Chat & invités :** **Unified Chat** (agrégation cross-plateforme, modération selon API, réponse, épinglage, overlay, traduction, relay mode) ; **Guest Studio** (invités navigateur sans installation : caméra/micro/partage d'écran, salle d'attente, ajout/retrait de scène).
  - **Enregistrement & rediffusion :** enregistrement **programme + ISO** (sources séparées) ; **Pre-Recorded Live** (diffuser une vidéo comme un direct : upload, playlists, boucle, programmation) ; **Scheduler** (calendrier, lancement/arrêt auto, rappels).
  - **Serveur Lunziko Live & Web :** **ingest/relay/redistribution/transcodage** ; **HLS/WebRTC/RTMP/SRT** ; **player web responsive** intégrable (embed) + page Live (player/chat/programme/réseaux) ; auth, API, statistiques, enregistrement, chat, gestion utilisateurs ; **administration web** (Dashboard/Streams/Users/Destinations/Storage/Network/Logs/Monitoring/Security) ; déploiement **Docker/Compose**.
  - **Virtual Camera :** caméra virtuelle du programme (Zoom/Teams/Meet/Discord…) — étude séparée Windows/macOS.
  - **Transverse :** **API versionnée** REST + **WebSocket** (streams/scenes/sources/destinations/recordings/schedule/chat/users/server) ; **plugins** (plateformes/protocoles/sources/filtres/transitions/effets/codecs/IA) ; **projets** & **workspaces** (Basic/Creator/Studio/Podcast/Gaming/Webinar/Church/Event/Vertical/Enterprise) ; **macros/automatisation** ; **monitoring** (CPU/GPU/RAM/FPS/dropped/bitrate/réseau/température/état destinations) + **gestion réseau adaptative** (bitrate/reconnexion/fallback) + **gestion d'erreurs** (récupération auto) ; **analytics** post-live ; **i18n** (FR+EN V1, architecture DE/IT/ES/PT/**lingála**/swahili) ; **accessibilité** ; **sécurité** (chiffrement secrets, TLS, RBAC, rotation tokens, audit, mode Diagnostic sans secrets).
- **Ressources / services requis :** **Lunziko Platform** (identité, **licences/achat** — plans Free/Personal/Creator/Pro/Studio/Enterprise pilotant destinations/résolution/caméras/ISO/IA/serveur/utilisateurs ; licence hors-ligne configurable ; signature ; bus/webhooks) ; **Lunziko HUB** (téléchargement/installation/MàJ/réparation/rollback/lancement + download-gating) ; **Lunziko AI Engine** (assistant scopé `live-stream` + LAIA : Director/Camera/Audio/Content, Brains video/vision/audio/voice/text, Action Registry, App Requirements, transcription/traduction/clips) ; **Lunziko Graphics Engine** (composition/overlays/chroma/shaders/rendu GPU) ; **Lunziko Design System** (tokens + **marque `live-stream` à ajouter au LDS**, comme `sentinel`). Dépendances techniques (clean-room) : FFmpeg, WebRTC, encodeurs matériels.
- **Expose :** état des streams/destinations, scènes/sources, enregistrements, chat unifié, player web/embed, événements `livestream.stream.started`/`livestream.destination.connected`/`livestream.recording.saved`/`livestream.clip.generated`… (bus Platform + webhooks) ; actions distantes (Action Registry). **Consomme :** **Platform** (identité/**licences**/signature/bus — autorité unique), **HUB** (distribution/MàJ/gating), **AI Engine** (assistant + LAIA multimédia), **Graphics Engine** (rendu/habillage temps réel), **Design System** (tokens + marque `live-stream`), plateformes de diffusion externes (OAuth/RTMP/SRT), flux caméras/serveurs.

## Suites et applications (§1.D)

### 1.D — Suites d'applications et leurs applications/modules

| Suite / Produit | Type | Applications / modules constitutifs |
|---|---|---|
| **DociaPub** | Suite documentaire (7 apps) | MyWord · MySheet · MySlides · MyMail · MyNotes · MyData · MyPublish |
| **Lunziko VidiaPub** | Suite créative (6 modules) | Publisher · Photo · PDF · Logo · Vidéo · Audio |
| **Lunziko One** | Suite ERP (12 modules, V1→V5) | Finance · Comptabilité · Ventes · Achats · Stocks · RH · Payroll · CRM · Projects · Manufacturing/Service · Analytics (client léger BI) · Marketplace |
| **Lunziko BI** | Application unique (multi-vues) | 21 vues desktop + 7 vues mobile (dashboards, rapports, gouvernance, lignage, simulation, IA, mobile AR/IoT/vocal…) |
| **Lunziko CAD** | Application unique | CAO Architecture + CAO mécanique (2D/3D paramétrique) |
| **Lunziko Yekoli** | Application unique | Web + mobile RN/Expo (Language Engine FSRS) |
| **Lunziko Sentinel** | Suite cybersécurité (agent + console web) | Agent desktop/serveur (Win/macOS/Linux) · Sentinel Mobile (Android/iOS) · **Sentinel Platform** (console web) · modules NGAV · EDR/XDR · Firewall · HIDS · Cloud Sandbox · Anti-vol · Vault · Remote Rescue |
| **Lunziko Live Stream** | Suite Live Media (desktop + mobile + serveur + web) | Live Stream Desktop (Win/macOS) · Live Stream Mobile/Lunziko Camera (iOS/Android) · **Lunziko Live Server** (Docker) · Lunziko Live Web (player/embed) · modules Studio/Scenes/Audio/Graphics/Multistream/Chat/Recording/Scheduler/Guests/AI Director/Virtual Camera/Analytics |
| **Lunziko Platform** | Socle / services (12 modules SDK) | Identity · Licensing · Subscription · Billing · AI Gateway · API · Security · Bus/Events · Trust&Signature · 3D Pipeline · Gaming Engine · Registre apps/logos |
| **Lunziko HUB** | Socle / distribution | Catalogue · Distribution/MàJ · Download-gating · Relations · Lanceur (Unified Manager) |
| **Lunziko AI Engine** | Socle / IA (**35 modules**, archi **LAIA**) | chat · embed · rag · memory · knowledge · agent · workflow · code · OpenAI-compat · ecosystem · activity · neural · data · assistant · handoff · **tools (A-4b)** · **mcp (A-7)** · **context (A-14→18)** · **feedback** · **catalog** · **automation (A-10)** · **actions** · **orchestrator (LAIA : Brains/Engines/Blackboard/Validation/Evaluation/Safety)** · **codeexec (A-11)** · **graphics (pont Graphics Engine)** · **connectors (RAG unifié)** · **safety** · **search (web keyless)** · **voice (TTS+STT+MT réels, packs, V-5 profils/voix fine-tunées)** · **learning (mémoire persistante « n'oublie jamais »)** · **autonomy (agent résilient « ne renonce jamais »)** · **models (catalogue modèles 2026)** · **code-intelligence (tous langages, écriture contrôlée+Git, multi-outils)** · **generation (Brains multimédias image/vidéo/3D)** · **reasoning (logiques OSS clean-room)** · +SDKs TS/Swift/Kotlin/Dart · **Ollama 100 % local validé** · 🐳 image Docker Hub `jaisebukadilu/lunziko-ai-engine:0.2.0` |
| **Lunziko Graphics Engine** | Socle / rendu (22 agents) | imaging · asset · vector · pdf · cad · bim · sketch · workflow · integrity · accel · vr · cache · shader · catalog … |
| **Lunziko Design System** | Socle / UI | tokens (8 cibles) · 19+ composants · 6 marques · densités · docs · CI |

> **Hors périmètre écosystème produit :** *ARC Église* (site client Next.js/Supabase), *MyOfficeSuite* (legacy Swift → devenu **DociaPub**), *LunzikoEngine/LCPEngine* (moteurs SPM macOS bas niveau). Non distribués via HUB en tant que produits Lunziko commerciaux.

## Règles de gouvernance (§0)

## 0. Règles de gouvernance (OBLIGATOIRES)

1. **Source de vérité = Lunziko Platform** pour les identités, licences, abonnements et **logos** des applications. **HUB se réfère en priorité à Platform**.
2. **Publication obligatoire.** Toute application Lunziko **doit communiquer ses informations** (manifeste : nom, catégorie, statut, fonctionnalités, ce qu'elle expose/consomme, logo, charte) aux **5 agrégateurs** de l'écosystème :
   - **Lunziko AI Engine** — pour raisonner sur l'écosystème et accompagner/résoudre les tâches de l'utilisateur ;
   - **Lunziko Design System** — pour disposer des bonnes informations d'UI/marque ;
   - **Lunziko Graphics Engine** — pour rendre au mieux (rendus, graphiques, visuels) ;
   - **Lunziko HUB** — pour tenir à jour la liste des applications téléchargeables ;
   - **Lunziko Platform** — pour la liste complète des applications, leurs logos, licences et services.
3. **Liste complète.** HUB, Platform, AI Engine et Graphics Engine **doivent posséder la liste complète** de toutes les applications (existantes **et à venir**) et de leurs fonctionnalités.
4. **Analyse au lancement (OBLIGATOIRE).** À **chaque lancement** des projets **HUB, Platform, AI Engine et Graphics Engine**, ce fichier **doit être analysé** pour **mettre à jour la mémoire** de ces projets.
5. **Traçabilité des ajouts.** À **chaque ajout d'une fonctionnalité** (ou d'une application) dans l'écosystème, l'entrée correspondante de ce registre **doit être mise à jour** (et consignée en mémoire projet).
6. **Finalité.** Rendre l'IA plus performante et capable de résoudre / accompagner l'utilisateur ; permettre à Graphics de donner son meilleur ; à Platform d'avoir la liste complète des apps + logos ; à HUB d'être à jour sur ses téléchargements ; au Design System d'avoir les bonnes informations.

> **Convention logo :** chaque projet contient un dossier `_logo/` (ou `_Logo/`). Le logo **société Lunziko** + le logo **produit** sont obligatoires (bas d'accueil) ; les logos des services connectés se greffent sur la même ligne. Platform agrège les logos de toutes les apps.

## Matrice de communication obligatoire (§2)

## 2. Matrice de communication obligatoire

> **Toutes les applications** doivent obligatoirement communiquer leurs informations à : **AI Engine · Design System · Graphics Engine · HUB · Platform.**

| Application ↓ / Agrégateur → | AI Engine | Design System | Graphics Engine | HUB | Platform |
|---|:---:|:---:|:---:|:---:|:---:|
| Lunziko One | ✓ | ✓ | ✓ | ✓ | ✓ |
| Lunziko BI | ✓ | ✓ | ✓ | ✓ | ✓ |
| DociaPub | ✓ | ✓ | ✓ | ✓ | ✓ |
| VidiaPub | ✓ | ✓ | ✓ | ✓ | ✓ |
| Lunziko CAD | ✓ | ✓ | ✓ | ✓ | ✓ |
| Lunziko Yekoli | ✓ | ✓ | ✓ | ✓ | ✓ |
| Lunziko Sentinel | ✓ | ✓ | ✓ | ✓ | ✓ |
| Lunziko Live Stream | ✓ | ✓ | ✓ | ✓ | ✓ |
| *(les agrégateurs se publient aussi mutuellement leurs capacités)* | ✓ | ✓ | ✓ | ✓ | ✓ |

**Informations communiquées (manifeste minimal par app) :** nom · catégorie · statut · **liste des fonctionnalités** · ce qu'elle **expose** / **consomme** · **logo** · charte/marque · version d'API (SemVer).
